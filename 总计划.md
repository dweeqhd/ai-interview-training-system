# AI 面试训练与表达分析系统：项目总计划

> 文档版本：V1.0  
> 项目类型：工程实训答辩项目  
> 建议周期：6 周（可按实际课程安排压缩）  
> 首版目标：完成中文、录音后分析、两个岗位的端到端可演示系统。

---

## 1. 项目一句话定义

本项目是一套面向大学生求职训练的 AI 面试辅助系统。用户选择目标岗位并录制回答后，系统会从**回答内容**和**表达流畅度**两个层面进行分析，输出有证据、可执行的改进建议，并记录多次训练的变化情况。

系统不做“是否能被录用”的预测，不对用户的外貌、性别、口音、年龄等特征评分；它的定位是**模拟训练和表达反馈工具**。

---

## 2. 首版范围：先做什么、不做什么

### 2.1 必做功能（MVP）

1. 用户选择岗位；
2. 系统展示对应的面试题；
3. 用户在网页中录音，或上传已有音频；
4. 系统完成中文语音转写；
5. 系统分析语速、停顿、语气词、回答时长；
6. 系统分析回答与题目的相关性、关键词覆盖和 STAR 结构；
7. 系统生成总分、雷达图、文本反馈和逐句问题定位；
8. 系统保存历史报告，并展示多次训练趋势。

### 2.2 第一版不要做

以下功能容易大幅增加工作量，建议仅写在“未来工作”中：

- 实时边说边识别；
- 虚拟数字人面试官；
- 表情、颜值、眼神、微表情评分；
- 自动判断“是否录用”；
- 同时覆盖很多岗位；
- 自己从零训练语音识别大模型；
- 简历解析、在线多人面试、微信小程序等。

### 2.3 推荐首发岗位

先只做两类岗位，每类 15–20 道题：

| 岗位 | 为什么适合首版 | 题目方向 |
|---|---|---|
| 软件开发/测试岗 | 团队熟悉，技术关键词容易构建 | 项目经历、技术栈、算法、数据库、协作与排错 |
| 产品/运营岗 | 可与开发岗形成对比，体现系统通用性 | 用户需求、数据分析、活动复盘、沟通协作 |

如果时间特别紧，只做“软件开发岗”也完全可以。答辩时应强调：系统架构支持扩展岗位，首版选择一个领域以保证题库和评分质量。

---

## 3. 最终交付物清单

项目结束前至少应准备好：

- 可运行的前后端系统；
- 20–40 道结构化面试题；
- 60–90 条自建模拟面试音频；
- 一份经过人工标注的小型评测集；
- 系统设计文档；
- 数据说明与隐私说明；
- 测试报告；
- 答辩 PPT；
- 一段不依赖网络也能播放的录屏备份；
- 2 组用于演示的标准案例：一段较弱回答、一段改进后的回答。

---

## 4. 产品流程

### 4.1 用户使用流程

~~~text
进入系统
  ↓
选择岗位、面试类型和题目
  ↓
阅读题目，点击开始录音
  ↓
结束录音并提交
  ↓
后台处理：音频预处理 → 语音分段 → 转写 → 指标计算 → 内容分析
  ↓
展示报告：原文、语速、停顿、STAR、关键词、建议
  ↓
用户查看历史记录并再次练习
~~~

### 4.2 一次回答的后台处理流程

~~~text
音频文件（WebM/MP3/WAV）
  ↓
格式统一：转换为 16 kHz、单声道 WAV
  ↓
VAD 语音活动检测：得到每段说话的开始/结束时间
  ↓
ASR 语音识别：得到转写文本、标点、字/句时间戳
  ↓
语音特征：语速、停顿、语气词、有效说话时长
  ↓
文本特征：关键词、题目相关性、STAR 结构、量化成果
  ↓
统一评分与反馈生成
  ↓
写入数据库，返回报告页
~~~

### 4.3 报告页的用户体验流程

1. 顶部先给出一段简短总结，例如“回答内容相关，但项目成果和个人职责说明不足”；
2. 展示总分和四维雷达图；
3. 分开呈现“表达分析”和“内容分析”，避免用户误解分数来源；
4. 展示完整转写文本，问题句子高亮；
5. 点击某处文本可查看相应时间段，必要时播放该段音频；
6. 最后给出 3 条优先级最高、可立即执行的建议；
7. 再次训练后显示本次与上次的指标对比。

---

## 5. 需求设计

### 5.1 用户角色

| 角色 | 权限 |
|---|---|
| 普通用户 | 选择岗位、练习、查看自己的报告和历史记录 |
| 管理员 | 新增岗位、题目、岗位关键词，查看匿名汇总数据 |

首版可以不做复杂注册，使用“昵称 + 本地会话”或简单账号即可。若已有学校统一登录能力，后续再接入。

### 5.2 面试题数据结构

每道题至少需要：

~~~json
{
  "id": "dev_project_01",
  "role": "software_developer",
  "category": "项目经历",
  "question": "请介绍一个你参与过的项目，并说明你负责的部分。",
  "expected_keywords": ["项目背景", "个人职责", "技术方案", "难点", "结果"],
  "star_expected": ["S", "T", "A", "R"],
  "suggested_duration_sec": [60, 120],
  "difficulty": "中等"
}
~~~

题库建立方式：

1. 先从真实招聘 JD 中整理软件开发和产品/运营岗位要求；
2. 将相似要求归类，如“前端开发、后端开发、数据库、项目协作”；
3. 每一类设计 3–5 个问题；
4. 为每个问题写出回答要点、关键词和常见失分点；
5. 请任课教师、学长学姐或同学审核题目是否合理；
6. 把问题写入数据库或 JSON 文件。

### 5.3 评分维度

首版使用 100 分制，权重固定且可配置：

| 一级维度 | 分值 | 二级维度 | 说明 |
|---|---:|---|---|
| 内容质量 | 60 | 题目相关性 20 分 | 是否回答了题目本身 |
|  |  | 结构完整性 20 分 | 是否包含 STAR 的必要部分 |
|  |  | 事实与成果 20 分 | 是否有个人职责、实例、量化结果 |
| 表达流畅度 | 40 | 语速适配 10 分 | 速度是否偏离该类题目的合理范围 |
|  |  | 停顿表现 15 分 | 是否有频繁或过长的句内停顿 |
|  |  | 语气词与时长 15 分 | 是否反复使用语气词、回答是否过短或过长 |

注意：固定阈值只能作为初始规则，不能被宣称为“通用面试标准”。系统应通过自建数据集得到同题或同类题的参考区间，并优先展示相对改进趋势。

---

## 6. 数据准备与标注流程

### 6.1 需要哪些数据

| 数据类型 | 建议规模 | 作用 | 是否需要训练模型 |
|---|---:|---|---|
| 面试题与岗位词库 | 2 岗位、各 15–20 题 | 出题和内容分析 | 否 |
| 模拟面试音频 | 20–30 人，每人 3 题 | 系统测试、参考区间 | 否 |
| 人工评分数据 | 30–50 条音频 | 验证系统评分合理性 | 否 |
| 停顿人工标注 | 20–30 条音频 | 验证停顿检测准确性 | 否 |
| 公开语音数据 | 可选 | 测试语音识别基线 | 否 |

语音识别使用预训练模型，不建议也不需要自行训练。公开中文语料只用于辅助验证；真实的模拟面试数据更能反映项目价值。

可选公开语料：

- AISHELL-1：普通话语音识别基准数据集，适合验证中文转写流程；
- Mozilla Common Voice 中文：包含公开授权的中文朗读语音；
- WenetSpeech：包含访谈等语体，但规模很大，首版不建议完整下载。

公开数据的授权、下载条件和使用范围必须在下载前再次核实。项目论文中应注明数据来源和用途。

### 6.2 自建模拟面试数据采集步骤

1. 写一份简短同意说明：录音仅用于课程项目、匿名统计和答辩演示；参与者可拒绝公开视频或要求删除；
2. 准备固定题单，保证不同参与者有可比性；
3. 建议每人回答 3 题，每题 60–120 秒；
4. 建议在安静室内、手机或电脑麦克风录制，保留少量有噪声样本做鲁棒性测试；
5. 文件统一命名：用户编号_岗位_题号_序号.wav，例如 U012_dev_Q03_01.wav；
6. 音频不要以真实姓名命名；
7. 每条音频记录题目、时长、录音环境和是否允许展示；
8. 原始录音仅限组内保存；答辩样例须再次确认展示许可。

### 6.3 人工评分与停顿标注

为了验证系统不是“自说自话”，从全部音频中随机选 30–50 条，安排两名标注者独立评分。

人工评分表字段建议如下：

~~~text
audio_id,role_id,question_id,duration_sec,
human_content_score_annotator_a,
human_fluency_score_annotator_a,
human_content_score_annotator_b,
human_fluency_score_annotator_b,
pause_start_sec,pause_end_sec,pause_type,note
~~~

评分规则应简单明确：

- 内容：是否答题、是否说明自己的职责、是否有具体行动和结果；
- 流畅度：表达是否连贯、是否频繁卡顿、是否有过多语气词；
- 停顿类型：句间停顿、句内停顿、录音干扰/无效片段。

标注原则：

- 先标记客观时间区间，再写主观评价；
- 两名标注者不互相查看评分；
- 对差异较大的样本进行讨论，形成最终标签；
- 不要根据音色、方言、性别等主观因素扣分。

### 6.4 数据划分

即使不训练模型，也要区分数据用途：

| 数据集 | 用途 |
|---|---|
| 开发集 | 调整停顿阈值、语气词词表、报告文案 |
| 测试集 | 最后评估，不再根据其结果反复修改规则 |
| 演示集 | 答辩用的稳定样例，不参与指标计算 |

一个可行划分是：60% 开发、20% 测试、20% 演示。确保同一位用户的同一段录音不会同时进入开发集和测试集。

---

## 7. 语音分析模块：具体怎么做

### 7.1 音频预处理

目的：让不同浏览器、手机和音频格式能进入同一分析流程。

处理步骤：

1. 前端录音得到 WebM，或用户上传 MP3/WAV；
2. 后端验证文件大小、时长和扩展名；
3. 转为 16 kHz、单声道 WAV；
4. 记录原始时长、采样率、声道数；
5. 对过短音频（如小于 5 秒）直接提示重新录制；
6. 将处理后的文件路径与回答记录关联。

建议设置：

- 单条回答最长 180 秒；
- 文件大小上限根据服务器配置设定；
- 不覆盖原文件；
- 转码失败时返回清晰错误提示。

### 7.2 VAD：识别说话与静音

VAD（Voice Activity Detection，语音活动检测）用于找出音频里“有人在说话”的片段。可直接使用 FunASR 配套的 fsmn-vad 或 Silero VAD，无需训练。

VAD 输出示例：

~~~json
[
  {"start_ms": 820, "end_ms": 2650},
  {"start_ms": 3200, "end_ms": 6040},
  {"start_ms": 6770, "end_ms": 9130}
]
~~~

处理规则：

1. 按时间排序所有语音段；
2. 去除特别短的噪声片段；
3. 合并间隔很短的片段，避免一个字被错误切断；
4. 相邻语音段的间隔就是候选停顿；
5. 句末停顿和句内停顿需要结合转写标点进一步分类。

初始参数可设置为：

| 参数 | 初始值 | 调整依据 |
|---|---:|---|
| 最短语音段 | 150 ms | 防止键盘声、爆破音被当作回答 |
| 合并间隔 | 200 ms | 避免正常说话被切成过多片段 |
| 明显停顿阈值 | 500 ms | 用自建数据逐步校准 |
| 长停顿阈值 | 1500 ms | 重点提示可能的表达卡顿 |

这些数值只是首版实验参数。最终报告应写成“经开发集对比后确定”，而不是写成放之四海皆准的结论。

### 7.3 ASR：语音转文字与时间戳

建议使用 FunASR 的中文 Paraformer 模型。其中文识别流程可搭配 VAD、标点恢复和时间戳能力，适合离线处理面试回答。

目标输出：

~~~json
{
  "text": "我在该项目中负责后端接口设计和数据库优化。",
  "sentences": [
    {
      "text": "我在该项目中负责后端接口设计和数据库优化。",
      "start_ms": 820,
      "end_ms": 6040
    }
  ],
  "char_timestamps": [
    {"char": "我", "start_ms": 820, "end_ms": 950}
  ]
}
~~~

实现注意事项：

- 先在 10–20 条真实普通话样本上验证转写效果；
- 人名、技术词和项目名可作为热词或自定义词表；
- 不要直接把 ASR 错字视为用户说错；
- 报告中应允许用户编辑转写文本；内容分析以“用户确认后的文本”为准更合理；
- 对于有明显方言、噪声或多人说话的音频，应提示“转写可信度可能下降”。

### 7.4 语速计算

定义：

~~~text
有效字数 N = 转写文本去掉标点、空格和语气词后的中文字符数量
实际说话时长 S = 所有 VAD 语音段时长之和（秒）
音频总时长 T = 音频完整时长（秒）

有效语速 = N / S × 60（字/分钟）
整体表达速度 = N / T × 60（字/分钟）
~~~

两个值都要展示：

- 有效语速反映真正说话时的快慢；
- 整体表达速度包含停顿，更贴近面试官听到的整体节奏。

不要仅凭某个固定数值判定“过快/过慢”。正确做法是：

1. 依据自建模拟面试数据，计算该题或该类别题目的中位数和分位数；
2. 将用户表现与参考区间比较；
3. 再结合停顿率共同判断；
4. 反馈使用“略快”“建议适当放缓”等温和表述。

### 7.5 停顿计算与分类

对于相邻两个语音段：

~~~text
gap_i = next_segment.start_ms - current_segment.end_ms
~~~

当 gap_i 大于等于 500 ms 时，把它记为明显停顿候选。

分类规则：

| 类型 | 判断方法 | 处理方式 |
|---|---|---|
| 句间合理停顿 | 前一段文本以句号、问号、感叹号等结束 | 统计但通常不扣分 |
| 句内停顿 | 前一段未结束，且后续仍是同一句 | 重点关注 |
| 长停顿 | 间隔超过 1.5 秒 | 在时间轴上高亮 |
| 无效停顿 | 明显是录音开始、结束或外界干扰 | 不纳入主要评分 |

最终至少计算：

~~~text
明显停顿次数
句内停顿次数
平均停顿时长
最长停顿时长
总停顿时长
停顿占比 = 总停顿时长 / 音频总时长
~~~

报告中的定位示例：

> 在 00:24–00:26 的“我主要负责……”之后出现 1.8 秒句内停顿。建议提前准备个人职责和技术方案的表述。

### 7.6 语气词与重复表达

建立可维护的词表，例如：

~~~text
嗯、啊、呃、额、那个、然后、就是、其实、怎么说呢、对吧
~~~

处理方法：

1. 先对文本进行同义归一化，例如“呃”“额”统一为“呃”；
2. 统计每个词的出现次数；
3. 计算每百字语气词数量；
4. 同时检查连续重复，如“然后然后”“就是就是”；
5. 当语气词集中出现于同一问题句时，提供针对性建议。

不要把“然后”在所有语境下都当成错误。可以按照“连续重复”“高频出现”“句内插入过多”三个条件综合判断。

---

## 8. 文本内容分析模块：具体怎么做

### 8.1 内容分析输入

每次分析至少传入：

~~~json
{
  "role": "software_developer",
  "question": "请介绍一个你参与过的项目，并说明你负责的部分。",
  "expected_keywords": ["项目背景", "个人职责", "技术方案", "难点", "结果"],
  "transcript": "……",
  "user_edited_transcript": true
}
~~~

### 8.2 第一层：规则分析

规则分析稳定、可解释，建议先实现。

#### A. 题目相关性

检查回答是否出现与题意有关的概念。

- 项目经历题：项目、负责、技术、难点、结果；
- 行为题：背景、冲突、行动、结果、反思；
- 技术题：概念解释、实现方式、优势/限制、实例。

可用“关键词词表 + 同义词词表”的方式，先不追求复杂模型。

#### B. STAR 结构识别

针对项目/行为类题，定义规则提示词：

| STAR 部分 | 可识别线索 |
|---|---|
| S 情境 | 当时、在……项目中、背景是、遇到了…… |
| T 任务 | 我的职责是、目标是、需要完成…… |
| A 行动 | 我采用、我设计、我实现、我协调、我优化…… |
| R 结果 | 最终、提升了、降低了、完成了、结果是、获得…… |

规则的目标不是“绝对准确”，而是为大模型分析和反馈提供可验证的基础。

#### C. 量化成果提取

使用正则和关键词检测数字、百分比、耗时、用户量、准确率等：

~~~text
提升 30%
响应时间从 2 秒降至 500 毫秒
负责 3 个模块
完成 20 次测试
~~~

如果用户回答缺少量化成果，反馈应为“如有条件，可补充成果数据”，不能逼迫用户编造数字。

### 8.3 第二层：大语言模型结构化分析

大模型用于理解同义表述、生成建议，但必须限制输出格式。

推荐提示词原则：

1. 明确角色是“面试训练助教”，不是招聘决策者；
2. 不评价外貌、身份、地域、口音等无关特征；
3. 只能根据题目、岗位要求和回答文本给建议；
4. 不可编造用户没有说过的项目经历；
5. 每个结论都需引用回答中的证据短句；
6. 输出严格 JSON，不直接输出大段自由文本。

建议 JSON 结构：

~~~json
{
  "relevance_score": 0,
  "star": {
    "s": {"present": true, "evidence": "……"},
    "t": {"present": false, "evidence": ""},
    "a": {"present": true, "evidence": "……"},
    "r": {"present": false, "evidence": ""}
  },
  "strengths": [
    {"point": "说明了使用的技术栈", "evidence": "……"}
  ],
  "gaps": [
    {"point": "没有说明个人职责边界", "evidence": "……"}
  ],
  "suggestions": [
    {
      "priority": 1,
      "action": "补充具体负责的模块和做出的技术决策",
      "example_pattern": "我负责……，为解决……，采用了……，最终……"
    }
  ]
}
~~~

### 8.4 内容分数合成

可用如下首版规则：

~~~text
内容分 = 题目相关性分 + STAR 完整性分 + 事实与成果分

题目相关性分：关键词/语义匹配结果归一化到 0–20
STAR 完整性分：S/T/A/R 每项 0–5 分，共 0–20
事实与成果分：个人职责、行动细节、量化成果各占一部分，共 0–20
~~~

人工评分样本收集后，应比较系统内容分与人工内容分的一致性。如果一致性较弱，不应简单修改总分，而要检查是关键词表、STAR 规则还是提示词存在问题。

---

## 9. 综合评分与反馈生成

### 9.1 推荐计算方式

~~~text
总分 = 内容质量分（0–60） + 表达流畅度分（0–40）
~~~

表达流畅度可进一步拆为：

~~~text
表达流畅度分 =
语速适配分（0–10） +
停顿表现分（0–15） +
语气词与时长分（0–15）
~~~

每一项都应记录原始指标和归一化后的分值。不要只保存总分，否则后续无法解释或调整。

### 9.2 反馈生成顺序

系统反馈应按优先级输出，避免一次给用户十几条建议：

1. 先指出 1–2 个优点；
2. 选择影响最大的 2–3 个问题；
3. 每个问题给出证据；
4. 每个问题给出一个可执行动作；
5. 对项目类、行为类题给一个结构模板。

示例：

~~~text
优点：回答覆盖了项目背景和所用技术，和软件开发岗位有较强相关性。

优先改进 1：个人职责不够明确。
证据：回答中多次使用“我们完成了”，但没有说明“我负责了什么”。
行动：把“我们开发了系统”改成“我负责后端接口与数据库索引设计”。

优先改进 2：缺少可验证的结果。
行动：补充性能、准确率、用户数量、完成周期等事实；若没有数据，可说明项目验收结果或个人收获。
~~~

### 9.3 报告可视化

报告页建议展示：

- 总分卡片；
- 四维雷达图：岗位相关性、结构完整性、表达流畅度、成果呈现；
- 语速数值与参考区间；
- 停顿时间轴；
- 语气词统计条形图；
- STAR 完整性卡片；
- 关键词命中标签；
- 转写文本和问题高亮；
- 本次与上次的趋势折线图。

---

## 10. 前端实现计划

### 10.1 页面清单

| 页面 | 主要内容 | 必须程度 |
|---|---|---|
| 首页 | 项目说明、开始训练入口 | 必须 |
| 岗位选择页 | 选择软件开发/产品运营、题目类型 | 必须 |
| 模拟面试页 | 题目、录音、计时、提交 | 必须 |
| 分析等待页 | 显示处理进度和失败提示 | 必须 |
| 报告页 | 分数、可视化、文本和建议 | 必须 |
| 历史记录页 | 历次报告和趋势对比 | 必须 |
| 管理页 | 题库、岗位词库维护 | 可选 |

### 10.2 模拟面试页实现步骤

1. 请求后端获取当前题目；
2. 调用浏览器 MediaRecorder 获取麦克风音频；
3. 录音时显示时长、停止按钮、重录按钮；
4. 停止后允许本地试听；
5. 点击提交，使用 multipart/form-data 上传音频和题目编号；
6. 后端返回任务编号；
7. 前端轮询任务状态，或使用 WebSocket 获取完成通知；
8. 完成后跳转到报告页。

首版以“提交后等待处理”代替实时识别，可显著降低复杂度。

### 10.3 报告页实现步骤

1. 按回答编号请求报告 JSON；
2. 使用图表库绘制雷达图、柱状图和趋势图；
3. 将停顿区间映射到音频播放时间轴；
4. 在转写文本中高亮对应句子；
5. 展示“优点”“优先改进项”“建议模板”；
6. 添加“再次练习此题”和“选择下一题”按钮。

---

## 11. 后端、数据库与接口设计

### 11.1 建议技术选型

| 层级 | 推荐方案 | 原因 |
|---|---|---|
| 前端 | Vue 3 + Element Plus + ECharts | 学习成本低，适合表单和图表 |
| 后端 | FastAPI | Python 生态方便接入语音和文本分析 |
| 数据库 | MySQL；原型阶段可用 SQLite | 保存结构化题库和报告 |
| 语音处理 | FunASR + FFmpeg | 中文转写、VAD、格式统一 |
| 文本分析 | Python 规则模块 + 可替换的大模型服务 | 能先做规则，再升级语义能力 |
| 文件存储 | 本地目录；后期可换对象存储 | 开发简单、便于隐私控制 |

### 11.2 数据库核心表

~~~text
users
  id, nickname, created_at

roles
  id, name, description

questions
  id, role_id, category, content, expected_keywords, star_expected, duration_min, duration_max

interview_sessions
  id, user_id, role_id, created_at, status

answers
  id, session_id, question_id, audio_path, audio_duration_sec, transcript, transcript_confirmed, created_at

audio_features
  id, answer_id, speech_duration_sec, effective_rate, overall_rate,
  pause_count, intra_sentence_pause_count, average_pause_sec,
  max_pause_sec, pause_ratio, filler_count, filler_rate

content_features
  id, answer_id, relevance_score, star_s, star_t, star_a, star_r,
  responsibility_score, result_score, keyword_hits_json, analysis_json

reports
  id, answer_id, total_score, content_score, fluency_score,
  strengths_json, suggestions_json, created_at
~~~

### 11.3 核心接口

| 方法 | 接口 | 功能 |
|---|---|---|
| GET | /api/roles | 获取岗位列表 |
| GET | /api/questions?role_id=... | 按岗位获取题目 |
| POST | /api/interview-sessions | 创建一次训练 |
| POST | /api/answers | 上传音频并提交回答 |
| GET | /api/analysis-tasks/{id} | 查询分析状态 |
| GET | /api/reports/{answer_id} | 获取分析报告 |
| GET | /api/users/{id}/history | 获取历史趋势 |
| POST | /api/admin/questions | 新增题目（管理员） |

上传回答接口示例：

~~~text
POST /api/answers
Content-Type: multipart/form-data

session_id: 1001
question_id: dev_project_01
audio: answer.webm
~~~

返回：

~~~json
{
  "answer_id": 3001,
  "task_id": "task_abc123",
  "status": "processing"
}
~~~

### 11.4 后台任务状态

任务状态建议为：

~~~text
uploaded → preprocessing → transcribing → analyzing → completed
                                      ↘ failed
~~~

失败时保存失败阶段和用户可读的错误信息，例如“音频过短”“转码失败”“语音识别服务暂不可用”，不要让页面一直转圈。

---

## 12. 开发目录建议

~~~text
ai-interview-system/
├─ frontend/
│  ├─ src/views/
│  ├─ src/components/
│  ├─ src/api/
│  └─ src/stores/
├─ backend/
│  ├─ app/
│  │  ├─ api/
│  │  ├─ services/
│  │  │  ├─ audio_preprocess.py
│  │  │  ├─ speech_analyzer.py
│  │  │  ├─ text_analyzer.py
│  │  │  ├─ scoring.py
│  │  │  └─ feedback_generator.py
│  │  ├─ models/
│  │  ├─ schemas/
│  │  └─ main.py
│  ├─ tests/
│  └─ requirements.txt
├─ data/
│  ├─ question_bank/
│  ├─ raw_audio/
│  ├─ processed_audio/
│  ├─ annotations/
│  └─ demo_samples/
├─ docs/
├─ scripts/
└─ README.md
~~~

原始音频、模型文件、密钥和数据库备份不能提交到公开代码仓库。应在 gitignore 文件中排除。

---

## 13. 六周开发排期

### 第 1 周：需求与数据规范

- 确定首发岗位和题目范围；
- 完成题库初稿；
- 定义评分标准、反馈语气和数据字段；
- 画出页面原型；
- 编写录音同意说明；
- 建立数据目录和文件命名规范。

验收：可以展示题库、页面原型、数据库 ER 草图和数据采集表。

### 第 2 周：系统骨架

- 初始化前端、后端项目；
- 设计数据库表；
- 完成岗位选择、题目展示、会话创建；
- 完成网页录音、上传和本地试听；
- 实现音频文件存储和基本错误处理。

验收：用户能选择题目、录音并成功上传。

### 第 3 周：语音分析

- 完成音频转码；
- 接入 VAD 和中文转写；
- 返回转写文本、时长和分段时间；
- 实现语速、停顿、语气词统计；
- 用 10–20 条样本人工检查结果。

验收：上传一段音频后，可得到正确的转写、语速和停顿数据。

### 第 4 周：内容分析与反馈

- 建立岗位关键词和同义词词表；
- 实现题目相关性、STAR 和量化成果规则；
- 接入结构化大模型分析或先完成规则版；
- 实现评分合成；
- 生成带证据的建议。

验收：报告能指出“个人职责缺失”“结果缺失”“关键词不足”等具体问题。

### 第 5 周：报告、数据集与评测

- 完成报告页、雷达图、时间轴、趋势图；
- 采集模拟面试音频；
- 进行人工评分与停顿标注；
- 对比人工分和系统分；
- 根据开发集结果校准阈值。

验收：系统可完成端到端流程，并有初步评测结果。

### 第 6 周：测试、完善与答辩

- 修复高频问题；
- 完成隐私说明和测试报告；
- 准备稳定演示样例；
- 撰写 PPT、录制备份视频；
- 进行至少两次完整答辩演练。

验收：断网或接口异常时也有录屏备份，所有图表和案例可稳定展示。

---

## 14. 团队分工

### 四人团队

| 成员 | 主职责 | 可交付成果 |
|---|---|---|
| A | 前端与交互 | 录音页、报告页、图表、历史页 |
| B | 后端与数据 | 数据库、接口、上传、任务状态、部署 |
| C | 语音算法 | 转码、VAD、ASR、语速停顿语气词分析 |
| D | 文本算法与评测 | 题库、STAR、提示词、数据标注、测试报告 |

### 三人团队

- A：前端与可视化；
- B：后端、数据库、音频处理；
- C：题库、文本分析、数据采集、评测。

所有成员共同负责：设计评审、数据采集、集成测试、PPT 和答辩。

---

## 15. 测试与效果评估

### 15.1 语音转写测试

从测试集选择 20 条左右音频，人工写出参考文本，检查：

- 是否能正确转写技术关键词；
- 是否能识别普通话和轻微口音；
- 噪声环境下是否出现明显错误；
- 用户修正文本后，内容分析是否更新。

可报告字符错误率（CER），但如果时间不够，至少给出“人工抽样正确率”和典型错误案例。

### 15.2 停顿检测测试

从人工标注集选择 20–30 条音频：

1. 人工标出明显停顿区间；
2. 系统输出候选停顿区间；
3. 判断系统是否识别到同一停顿；
4. 统计准确率、召回率或平均时间误差；
5. 分析误差来源，如呼吸声、环境噪声、说话尾音。

不要编造指标。若结果一般，应如实说明“在嘈杂环境下存在误判”，并展示对应优化方案。

### 15.3 内容评分测试

对同一批回答：

1. 两名同学按固定规则独立给出内容分与流畅度分；
2. 取两人平均值或讨论后结果作为参考；
3. 计算系统分与人工分的差异；
4. 关注排序是否合理：较完整的回答是否通常比极短、跑题回答得分更高；
5. 展示 2–3 个系统与人工一致、一个不一致的案例。

### 15.4 产品测试

| 测试类别 | 示例 |
|---|---|
| 功能测试 | 录音、重录、上传、报告、历史记录是否正常 |
| 异常测试 | 空音频、超长文件、断网、格式不支持、分析失败 |
| 兼容测试 | Chrome、Edge；不同麦克风与音频格式 |
| 性能测试 | 多条音频连续提交、处理时间是否可接受 |
| 可用性测试 | 找 5 名同学完成一次练习，收集理解难点 |
| 隐私测试 | 访问他人报告、删除音频、日志泄漏等 |

---

## 16. 隐私、安全与伦理说明

答辩时建议单独用一页 PPT 说明：

- 用户上传音频仅用于生成训练报告；
- 采集数据前取得参与者同意；
- 原始音频不公开、不用于商业用途；
- 支持删除个人训练记录和音频；
- 报告是训练反馈，不构成招聘决定；
- 不以性别、年龄、方言、外貌、种族等因素作为评价依据；
- 大模型只根据题目、岗位要求和回答文本分析，不编造经历。

这部分会显著提升项目的完整性与可信度。

---

## 17. 常见风险与备选方案

| 风险 | 处理方案 |
|---|---|
| 本地电脑没有足够算力 | 将语音识别与大模型封装为可替换服务；开发期用少量音频异步处理 |
| ASR 转写错技术词 | 建立岗位热词表；允许用户编辑转写文本 |
| 大模型输出不稳定 | 强制 JSON 格式；先做规则版；保存原始结果以便排查 |
| 数据量不足 | 重点做小而精的自建评测集，不盲目下载大规模公开数据 |
| 停顿阈值不合理 | 用开发集标注样本调整，不使用绝对化结论 |
| 演示网络不可用 | 准备已生成的报告、录屏和本地演示样例 |
| 开发进度落后 | 保留“录音后分析”主线，砍掉实时、视频、简历解析等扩展功能 |

---

## 18. 答辩材料与演示脚本

### 18.1 PPT 建议结构

1. 项目背景：大学生缺少低成本、可重复的面试训练；
2. 痛点：普通模拟面试反馈慢、反馈不够量化；
3. 项目目标与边界；
4. 系统总体架构；
5. 语音分析方法；
6. 内容分析与 STAR 方法；
7. 数据集与人工标注；
8. 系统页面与核心流程；
9. 实验与效果评估；
10. 创新点；
11. 隐私与局限性；
12. 演示与未来扩展。

### 18.2 推荐演示脚本

1. 选择“软件开发岗”；
2. 展示题目：“请介绍一个你参与过的项目，并说明你负责的部分。”；
3. 上传或录制一段 60–90 秒的准备好的回答；
4. 等待分析完成，展示转写结果；
5. 展示停顿时间轴、语速、语气词；
6. 展示 STAR 缺失项、岗位关键词和具体建议；
7. 展示同一用户改进后第二次回答；
8. 在历史图中展示停顿下降、结构分上升；
9. 总结系统不是替代面试官，而是帮助用户形成“练习—反馈—改进”的闭环。

### 18.3 创新点表述

可以使用以下表述：

> 本系统不是仅做语音转写或普通问答，而是将回答内容结构与语音节奏进行对齐分析：一方面识别岗位相关性、STAR 结构和成果表达，另一方面定位句内停顿、语速和语气词，并结合用户多次训练记录生成可解释、可执行的个性化反馈。

---

## 19. 最终验收检查表

### 功能

- [ ] 能选择岗位和题目；
- [ ] 能录音、重录、上传、试听；
- [ ] 能成功转写中文音频；
- [ ] 能展示语速、停顿、语气词；
- [ ] 能展示 STAR 和岗位关键词分析；
- [ ] 能生成有证据的改进建议；
- [ ] 能保存和查看历史报告；
- [ ] 异常音频有清晰提示。

### 数据与实验

- [ ] 题库有来源和人工审核；
- [ ] 自建音频数据有使用同意；
- [ ] 有人工评分与停顿标注表；
- [ ] 有系统与人工结果对比；
- [ ] 所有实验数据和指标真实可复现。

### 答辩

- [ ] 演示案例稳定；
- [ ] 有录屏备份；
- [ ] 能解释每个分数的来源；
- [ ] 能解释为何不做表情/录用预测；
- [ ] 能说明数据隐私和项目局限；
- [ ] 每位组员都有明确负责模块。

---

## 20. 建议立即开始的第一批任务

按优先顺序执行：

1. 确定首发岗位：默认选择“软件开发岗”；
2. 团队一起写出 15 道题及其关键词、STAR 要点；
3. 完成评分表与录音同意说明；
4. 采集 5 条内部测试录音；
5. 搭建“录音上传 → 后端接收 → 返回结果”的最小流程；
6. 接入转写与停顿统计；
7. 再开始做漂亮的报告页和大模型内容建议。

先跑通一条完整链路，再持续加功能。这样即使后期时间不足，也已经有一个可答辩、可演示的完整系统。
