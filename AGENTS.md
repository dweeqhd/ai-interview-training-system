# 项目协作规则

本文件约束所有后续 AI 辅助开发工作。

## 修改前必须完成

1. 通读根目录 README.md。
2. 通读根目录 代码现状分析.md。
3. 使用文件清单检查当前仓库全部文件。
4. 通读本次修改涉及的全部代码及其直接调用方。
5. 检查 Git 状态，保留用户已有修改，不覆盖无关内容。

## 修改中必须遵守

1. 只实现当前阶段或当前任务明确要求的内容。
2. 真实音频、人工标注、密钥、模型文件和本地数据库不得提交。
3. 所有评分必须保留原始指标和计算依据。
4. 不使用外貌、性别、年龄、方言等信息进行评价。
5. 不生成“是否录用”的结论。
6. 未经用户同意，不安装软件、不修改 PATH、不创建虚拟环境、不下载模型、不写入 API 密钥。

## 提交前必须完成

1. 运行与改动相称的测试或格式检查。
2. 更新根目录 代码现状分析.md，写清：
   - 当前可运行状态；
   - 本次修改内容；
   - 验证结果；
   - 当前限制和风险；
   - 下一步工作；
   - 需要用户完成的配置或数据准备。
3. 查看 Git 差异，确认没有隐私数据和密钥。
4. 再创建 Git 提交。

