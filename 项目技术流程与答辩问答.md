# AI 面试训练与表达分析系统：技术流程与答辩问答

> 整理日期：2026-09-10
> 当前 API：0.3.3
> 当前内容规则：`rules-v2.1-dev30-syn18-resp-sync`
> 项目定位：面向大学生的中文模拟面试训练工具，不是招聘录用决策系统

## 一、30 秒项目介绍

本项目是一套本地运行的中文 AI 面试训练系统。用户选择软件开发岗位的题目后，可以直接在网页录音或上传音频。后端先使用 FFmpeg 统一音频格式，再通过 FunASR 调用本地预训练的 Paraformer、FSMN-VAD 和 CT-Punc 模型，完成中文转写、语音活动检测、标点恢复和字词时间戳提取。系统在模型输出之上计算语速、停顿、语气词，并用可解释的规则分析题目要点、STAR 结构、个人职责、具体行动和项目结果，最终在页面展示分数、证据句、雷达图、停顿时间轴和改进建议。

我们没有从零训练或微调语音模型。30 条人工标注录音被用作开发集，用来评测预训练模型和规则、发现错误并校准同义词，而不是直接更新神经网络参数。

## 二、系统解决什么问题

普通面试练习主要依赖个人感觉，用户很难准确发现以下问题：

- 回答有没有真正覆盖题目要求；
- 是否说明了自己的职责，而不是只说“我们做了什么”；
- STAR 结构是否完整；
- 是否存在较长停顿、语气词过多或表达节奏不稳定；
- 多次练习后是否有进步。

本系统把一次回答转化为可复核的文本证据和语音指标。它强调“训练反馈”和“个人纵向比较”，不预测能否被录用，也不评价外貌、性别、年龄、方言或口音。

## 三、总体技术架构

```text
浏览器（Vue 3）
  ├─ 题库选择与分类筛选
  ├─ MediaRecorder 网页录音 / 本地文件上传
  ├─ 每 2 秒查询一次分析状态
  └─ 报告、转写修正、历史趋势展示
             │ HTTP / JSON / multipart
             ▼
FastAPI 后端
  ├─ 文件格式、大小、时长校验
  ├─ 后台分析任务
  ├─ FFmpeg 音频标准化
  ├─ FunASR 本地语音模型推理
  ├─ 语音指标计算
  ├─ 可解释内容规则评分
  └─ SQLAlchemy 写入 SQLite
             │
             ▼
本机文件与数据
  ├─ 原始音频与 16 kHz WAV
  ├─ 本地预训练模型
  ├─ SQLite 历史记录
  └─ 人工标注、评测缓存与匿名汇总
```

## 四、采用的主要技术

| 层次 | 技术 | 当前版本或配置 | 主要作用 |
|---|---|---|---|
| 前端 | Vue | 3.5.42 | 题库、录音、状态与报告交互 |
| 前端构建 | Vite | 8.2.2 | 本地开发服务器与前端构建 |
| 图表 | ECharts | 6.1.0 | 图表能力；当前报告还包含自绘 SVG 雷达图和 CSS 趋势图 |
| 浏览器录音 | MediaRecorder API | 优先 WebM/Opus | 直接采集网页录音并在提交前试听 |
| 后端 | FastAPI / Uvicorn | 0.141.1 / 0.52.4 | REST API、文件上传和后台任务 |
| 数据校验 | Pydantic | 随 FastAPI 环境 | 请求和响应结构校验 |
| 数据访问 | SQLAlchemy | 2.0.52 | 操作会话、回答、任务和报告记录 |
| 数据库 | SQLite | 本机文件数据库 | 保存训练历史、转写、指标和报告 |
| 音频处理 | FFmpeg / FFprobe | 9.0.1 | 格式转换、时长读取和 WebM 兼容 |
| 语音框架 | FunASR | 1.4.15 | 统一调用 ASR、VAD 和标点模型 |
| 模型来源 | ModelScope | 1.40.0 | 下载和管理本地模型缓存 |
| 推理框架 | PyTorch / torchaudio | 2.11.0 CPU | 在 CPU 上执行模型推理 |
| 自动测试 | pytest / HTTPX | 9.1.1 / 0.28.1 | 后端规则、接口和评测回归测试 |

## 五、一次录音的完整处理流程

### 1. 前端采集

1. 用户选择岗位和题目；
2. 浏览器请求麦克风权限；
3. `MediaRecorder` 优先录制 WebM/Opus，不支持时尝试浏览器可用格式；
4. 停止后生成本地 Blob，用户可以先试听或重新录制；
5. 点击“提交并分析”后，以表单形式上传题目编号和音频。

### 2. 上传校验

后端检查：

- 格式是否属于 WAV、WebM、MP3、M4A、OGG、FLAC；
- 文件是否为空；
- 是否超过 25MB；
- 时长是否在 5–180 秒内。

浏览器流式生成的 WebM 有时没有容器级总时长。系统会先读取 `format.duration`，若缺失或无效，则根据首尾音频包时间戳计算真实时长，避免把可播放文件误判为损坏。

### 3. 音频标准化

FFmpeg 将不同来源的音频统一转换成：

```text
16 kHz + 单声道 + 16 位 PCM WAV
```

这样可以减少浏览器、手机、格式和采样率差异对后续模型的影响，同时保留原始文件用于问题复查。

### 4. 本地语音模型推理

FunASR 的 `AutoModel` 同时组合三个预训练模型：

- Paraformer：把中文语音转换为文字，并给出字词时间戳；
- FSMN-VAD：识别哪些时间段存在语音；
- CT-Punc：为识别文本恢复标点。

所有模型文件都保存在项目的 `models` 目录，推理发生在本机，不调用 GPT，也不把录音自动上传到云端。

### 5. 语音指标计算

系统根据转写与字词时间戳计算：

```text
表达单位数 = 中文字符数 + 英文/数字连续词组数
有效语速 = 表达单位数 ÷ 有效说话时长 × 60
停顿时长 = 下一语音段开始时间 - 上一语音段结束时间
停顿占比 = 明显停顿总时长 ÷ 音频总时长
```

当前把不少于 500ms 的空隙视为明显停顿，不少于 1500ms 的空隙视为长停顿。系统还统计“嗯、呃、额、啊、那个、就是、然后”等语气词。

### 6. 内容规则分析

内容分析不是让大语言模型直接给分，而是执行可复现的规则：

- 题目相关性：检查题目要点及同义线索；
- 结构完整性：经历题检测 STAR，技术题检测“观点—方法—权衡—验证”；
- 事实与成果：检查个人职责、具体行动、结果/验证和量化事实；
- 每个命中都保存触发词和回答中的证据句；
- 最终生成最多两条优点和三条优先改进建议。

当前分数结构为：

```text
内容分 60 = 题目相关性 20 + 结构完整性 20 + 事实与成果 20
表达分 40 = 表达语速 10 + 停顿表现 15 + 语气词与时长 15
总分 100 = 内容分 + 表达分
```

### 7. 保存和展示

SQLite 保存会话、回答、后台任务、模型原始转写、当前转写、语音指标和报告。前端每 2 秒查询任务状态；分析完成后显示报告，并把记录加入本机历史页。

## 六、是否训练了模型

### 准确回答

没有训练或微调语音神经网络，也没有训练大语言模型。项目直接使用已有的中文预训练模型进行离线迁移使用：

| 模型 | 来源/标识 | 用途 | 是否被本项目微调 |
|---|---|---|---|
| Paraformer | `iic/speech_seaco_paraformer_large_asr_nat-zh-cn-16k-common-vocab8404-pytorch` | 中文 ASR 与时间戳 | 否 |
| FSMN-VAD | `iic/speech_fsmn_vad_zh-cn-16k-common-pytorch` | 语音活动检测 | 否 |
| CT-Punc | `iic/punc_ct-transformer_cn-en-common-vocab471067-large` | 标点恢复 | 否 |

三个模型合计约 2.04GB。当前通过 PyTorch CPU 推理，代码中配置为 `device="cpu"`。

### 30 条录音为什么不用于微调

30 条数据规模过小，而且只有一名标注员。如果直接微调大模型，容易过拟合这 5 名参与者的声音、题目和录音环境，无法证明对新用户有效。课程项目当前更合理的做法是：

1. 使用成熟预训练模型作为语音基线；
2. 用真实面试录音评测它在本项目场景下的表现；
3. 在模型输出上设计面向面试训练的可解释算法；
4. 通过独立测试集验证，而不是用小数据强行宣称完成模型训练。

### 可以如何描述“迁移使用与创新”

可以说我们进行了“预训练模型的场景化迁移与二次算法开发”，但不能说“训练了自己的语音模型”。具体包括：

- 把通用中文转写能力迁移到模拟面试回答场景；
- 不只使用最终文本，还利用 Paraformer 的字词时间戳构造语音段；
- 从相邻字词段间隔推导面试表达停顿和停顿时间轴；
- 将语音指标、题目要点和证据句组合成可解释报告；
- 支持人工修正 ASR 后重新计算内容分，同时保留原始模型输出。

## 七、采集了什么数据，如何使用

### 1. 真人开发集

当前已完成：

- 30 条匿名模拟面试录音；
- 5 名参与者；
- 3 道软件开发类题目，每题 10 条回答；
- 1 名标注员 L01；
- 全部作为开发集，不作为最终独立测试集。

标注内容包括：

- 录音元数据；
- ASR 原文和人工修正转写；
- 停顿起止时间、时长、类型与有效性；
- 内容证据是否出现；
- 题目相关、结构、事实成果、语速、停顿、语气词与时长六维人工评分；
- 共识评分表目前为空，因为没有第二名独立标注员。

### 2. AI 合成控制集

项目另有 18 条明确标记为 `synthetic_ai` 的文本控制样例，共 136 个布尔断言，用于覆盖跑题、关键词堆砌、缺少结构、职责边界等正反例。

这些文本只用于规则回归测试：不冒充真人录音、不冒充第二名标注员，也不参与声学指标评测。

### 3. 数据的实际用途

| 数据 | 如何使用 | 是否更新模型参数 |
|---|---|---|
| 原始录音 | 重新运行本地 ASR、时间戳和语音指标 | 否 |
| 人工修正转写 | 计算 ASR 字符错误率 CER；隔离转写误差与规则误差 | 否 |
| 人工停顿 | 与系统停顿一对一匹配，计算 Precision、Recall、F1，并扫描阈值 | 否 |
| 内容证据标注 | 检查题目要点、职责、行动和结果是否漏报 | 否 |
| 六维人工评分 | 计算系统分数与人工分数的 MAE、偏差和 Pearson 相关系数 | 否 |
| 18 条合成控制文本 | 检查改规则后是否引入明显误报或回归 | 否 |

### 4. 数据带来的实际改进

- 发现只使用整句边界会漏掉句内卡顿，改为利用字词时间戳；
- 根据真实口语补充“独立负责、负责后端、符合预期、未再出现”等同义线索；
- 发现左右两套职责词表不一致，现已统一复用同一职责证据词表；
- 增加人工修正转写和重新评分功能，降低 ASR 错词对内容反馈的影响；
- 识别出当前停顿规则、语速区间和内容排序能力仍不够成熟，没有隐瞒低指标。

### 5. 当前评测结果

| 指标 | 当前结果 | 如何解释 |
|---|---:|---|
| ASR 语料级 CER | 18.39% | 每 100 个参考字符约有 18 个编辑错误，技术词仍可能识别错误 |
| 样本平均 CER | 19.67% | 对 30 条样本分别计算后取平均 |
| 人工修正文本内容分 MAE | 9.57 | 规则内容总分与人工内容总分平均相差约 9.57 分（满分 60） |
| 原始 ASR 文本内容分 MAE | 10.37 | 包含转写错误后的端到端结果 |
| 内容证据正例召回 | 79/80，98.75% | 已标为“有”的证据多数能找到；因缺少负例，不能据此声称准确率 98.75% |
| 表达总分 MAE | 5.33 | 系统表达分与人工表达分平均差约 5.33 分（满分 40） |
| 停顿 Precision / Recall / F1 | 0.1520 / 0.6458 / 0.2460 | 系统找到部分人工停顿，但额外检出很多，仍需改进 |
| 内容总分 Pearson | 0.0647（人工修正文） | 排序相关性很低，不能声称系统已经具备通用评分能力 |

## 八、项目的创新点

### 可以合理陈述的创新

1. **可解释而不是黑盒给分**：每项内容结论同时展示触发线索和原文证据，用户可以知道为什么得分或失分。
2. **字词级停顿分析**：使用预训练 ASR 的字词时间戳发现句内卡顿，而不是只根据整句边界或音频总静音判断。
3. **人机闭环修正**：保存模型原始转写，允许用户修正文字，并只重新计算受文本影响的内容报告；原始语音指标不会被伪造或覆盖。
4. **同一语义只维护一份职责词表**：题目要点和事实证据统一复用职责线索，减少同一句话在页面两处结论冲突。
5. **用自建标注数据验证系统**：不仅展示页面，还实际计算 CER、MAE、相关系数和停顿 F1，明确哪些部分有效、哪些部分不足。
6. **合成控制与真人数据分离**：AI 样例只做回归和负例覆盖，不把合成数据包装成真人或人工共识。
7. **本地隐私设计**：录音、转写、模型和数据库均留在本机，默认不调用云端服务。
8. **浏览器 WebM 容错**：针对 MediaRecorder 缺少容器总时长的实际兼容问题，使用音频包时间戳回退计算。

### 不应夸大的地方

- 不是提出了新的 ASR 神经网络结构；
- 没有完成语音模型微调；
- 当前规则不是经过大规模验证的行业评分标准；
- 不能用当前系统决定候选人是否录用；
- 不能把 98.75% 正例召回表述成 98.75% 综合准确率。

## 九、这些能力在页面上如何体现

| 页面区域 | 后端数据或算法 | 用户看到的效果 |
|---|---|---|
| 岗位与题库 | 结构化 JSON 题库 | 软件开发岗 15 道题、题型筛选、建议时长和回答要点 |
| 录音区 | MediaRecorder、文件校验 | 开始/停止录音、试听、重录、上传和错误提示 |
| 分析状态 | FastAPI 后台任务 | 上传中、排队中、处理中、完成或失败 |
| 回答原文 | Paraformer ASR | 中文转写；用户可修改并点击“保存并重新评分” |
| 总分和分数拆解 | 100 分规则基线 | 内容/表达及六个二级分，并显示计算依据 |
| 四维雷达图 | 各分项归一化 | 题目相关、结构完整、事实成果、表达流畅 |
| 题目要点 | 题目词表和同义线索 | 每个要点是否命中以及对应证据句 |
| STAR/逻辑结构 | 结构提示词规则 | S/T/A/R 或观点/方法/权衡/验证是否出现 |
| 事实证据 | 职责、行动、结果和数字检测 | 哪些事实已经表达、哪些需要补充 |
| 表达节奏 | 字词时间戳与统计 | 有效语速、停顿次数、最长停顿、语气词 |
| 停顿时间轴 | 停顿起止时间 | 在整段回答中的相对位置显示普通/长停顿 |
| 优点和建议 | 缺失项优先级规则 | 最多两条优点和三条可执行建议 |
| 历史页 | SQLite 报告记录 | 打开旧报告，查看同题最近 8 次趋势 |

## 十、系统是不是只能在本地运行

### 当前事实

当前版本只完成并测试了本地单机运行：

- 前端地址是 `http://localhost:5173`；
- 后端地址是 `http://127.0.0.1:8000`；
- Vite 代理固定指向本机后端；
- 后端 CORS 只允许本机前端地址；
- SQLite、模型、录音和历史记录均在运行该项目的电脑上；
- 当前没有账号系统、远程同步、HTTPS 和服务端权限控制。

因此现在不能把这台电脑关掉后，继续从其他电脑访问它。

### 是否被 `D:\learning\工程实训\AI面试训练与表达分析系统` 绑定

不是。代码通过项目根目录计算模型、数据库和音频的相对位置，并不依赖这个固定 D 盘路径。把完整项目、依赖和模型复制到另一台兼容的 Windows 电脑后，可以重新启动。

更准确的答辩说法是：“当前交付版本是本地单机部署，但架构并非只能放在这个 D 盘目录。”

### 如何改成局域网或云端系统

需要完成：

1. 把后端监听地址改为服务器网卡地址；
2. 配置正式前端地址、反向代理和 CORS；
3. 在服务器部署模型和 FFmpeg；
4. 将 SQLite 升级为适合多用户的数据库；
5. 增加账号、权限、HTTPS、数据删除和保留周期；
6. 把同进程后台任务升级成独立任务队列；
7. 明确录音上传、隐私同意和访问审计规则。

远程部署意味着录音需要传到服务器，隐私风险会高于当前纯本地方案。

## 十一、文字转写是如何实现的

文字转写流程不是调用 GPT：

```text
WebM/M4A/WAV
  → FFmpeg 转换成 16 kHz 单声道 WAV
  → FunASR AutoModel 加载本地 Paraformer
  → FSMN-VAD 辅助定位语音段
  → Paraformer 解码中文并输出字词时间戳
  → CT-Punc 恢复标点
  → 保存为模型原始转写
  → 内容规则读取转写并生成报告
```

如果技术词识别错误，用户可以在报告页直接修改转写。系统保留原始 ASR 文本，并用修正后的文本重新计算题目相关性、结构和事实证据；停顿和语速等原始语音指标仍来自录音，不随文字编辑而伪造变化。

## 十二、当前处理时间与改进办法

### 当前观察

当前使用 i7-13620H、约 16GB 内存和 CPU 版 PyTorch。本机数据库现有 8 条已完成任务的观察时间为：

- 任务耗时范围约 3.6–241.9 秒；
- 平均约 72.7 秒；
- 中位数约 38.3 秒；
- 音频长度不同，首次模型加载也会显著增加等待，因此这些数据不是严格性能基准。

主要耗时不在规则评分，而在模型加载和 ASR 推理。规则评分通常很快。

### 可以如何改善

| 优先级 | 方法 | 能改善什么 | 代价或注意点 |
|---|---|---|---|
| 1 | 服务启动时预热模型 | 避免第一位用户承担冷启动等待 | 启动服务本身会更慢，占用内存更早 |
| 2 | 保持后端常驻、不要每次重启 | 后续录音复用已加载模型 | 只解决冷启动，不减少全部推理量 |
| 3 | 使用 NVIDIA GPU 推理 | 缩短 Paraformer 推理时间 | 需安装匹配的 CUDA/PyTorch，代码把 `device` 改为可配置并实测 |
| 4 | 选择更小的 ASR 模型 | 降低 CPU 时间和内存 | 识别准确率可能下降，必须重新测 CER |
| 5 | 模型量化或 ONNX 等推理优化 | 可能提升 CPU 推理效率 | 实施和兼容成本较高，需要基准测试 |
| 6 | 独立任务队列/工作进程 | 多用户时提高可靠性和并发体验 | 不一定降低单条录音的纯推理时间 |
| 7 | 页面增加阶段进度和预计时间 | 降低等待焦虑 | 只改善体验，不直接提升算力 |

最适合当前答辩版本的短期方案是“启动时预热 + 后端常驻”。如果仍不能满足演示速度，再考虑 GPU 推理；租 GPU 是为了加速推理，不代表必须训练模型。

## 十三、当前限制

1. 目前只有软件开发一个岗位和 15 道题，题库仍需教师或行业人员审核；
2. 只有 30 条开发集录音、5 名参与者和 1 名标注员，不能证明对所有人群和场景有效；
3. 尚缺按参与者隔离的独立测试集；
4. 内容证据人工标签全部为“是”，只能估计召回，不能可靠估计误报率；
5. ASR CER 为 18.39%，项目名、英文缩写和技术词容易识别错误；
6. 内容总分与人工评分的 Pearson 相关性很低，规则不适合做人才排序；
7. 停顿 F1 只有 0.2460，呼吸声、拖音和人工定义差异仍会造成误检；
8. 当前 120–300 表达单位/分钟区间过宽，30 条开发录音都获得语速满分，区分度不足；
9. 关键词命中不代表用户真正掌握技术，数字出现也不一定是有效成果；
10. 当前 CPU 推理存在冷启动和长录音等待；
11. 只有单机 SQLite，没有账号隔离、多用户并发和云端同步；
12. 后台任务和 Web 服务在同一进程，服务中断后任务不能自动恢复；
13. 暂无训练记录删除入口、正式访问控制和数据保留策略；
14. 模型目前使用下载时的 `master` 修订，最终归档前应固定模型版本并复核许可证；
15. 系统仅用于训练反馈，不能给出录用结论。

## 十四、老师可能提问及建议回答

### 1. 你们这个项目真正用了 AI 吗？

用了。语音转写、语音活动检测和标点恢复使用了本地预训练模型；但内容评分目前是可解释规则，不是大语言模型黑盒评分。我们选择这种组合是为了离线运行、保护录音隐私，并且让每项结论都有证据。

### 2. 你们训练模型了吗？

没有更新神经网络参数。我们使用 Paraformer、FSMN-VAD 和 CT-Punc 预训练模型，并在它们输出的文本和时间戳之上开发面试场景算法。30 条录音用于评测和规则校准，不用于微调。小样本直接微调容易过拟合，所以我们没有把“使用模型”夸大成“训练模型”。

### 3. 既然没训练模型，30 条录音有什么价值？

它们用于建立项目自己的开发集。我们用人工转写测 CER，用人工停顿测 Precision、Recall 和 F1，用六维人工评分测 MAE 和相关性，并据此发现句内停顿遗漏、同义词漏报和语速阈值过宽等问题。数据真正推动了算法修改和限制分析。

### 4. 为什么不直接让 GPT 给面试分数？

录音和转写涉及隐私，直接发送到云端需要费用、授权和网络；同时自由文本评分难以稳定复现。当前规则基线能够离线运行，并展示每个触发词和证据句。未来可以把大模型作为辅助建议层，但仍需结构化输出、证据约束和人工复核。

### 5. 你们的创新在哪里？

创新主要是应用和算法组合，而不是提出新的神经网络。我们利用预训练模型的字词时间戳进行句内停顿分析，将语音和文本证据组合成可解释报告，支持人工纠正转写后重新评分，并建立真人标注评测、合成回归控制和规则版本管理闭环。

### 6. 为什么左侧“个人职责”和右侧曾经不一致？

原来两处使用不同词表，“独立负责”只存在于事实证据词表，题目要点词表没有，所以一边命中、一边漏报。现在“个人职责、个人责任、个人任务”统一复用同一职责证据词表，并增加了回归测试；规则版本也已更新，避免不同版本分数混用。

### 7. 分数是怎么计算的？

总分 100 分，其中内容 60 分、表达 40 分。内容包含题目相关性、结构完整性和事实成果，各 20 分；表达包含语速 10 分、停顿 15 分、语气词与时长 15 分。页面保留每个分项、原始指标、命中词和证据句，所以它是可解释的规则基线。

### 8. 这个分数可靠吗？

目前只能用于训练反馈和个人纵向比较，不能用于录用或人才排序。当前内容 MAE 有所下降，但排序相关性低；停顿 F1 也较低。我们主动展示这些限制，并计划使用新参与者的独立测试集验证。

### 9. 98.75% 是准确率吗？

不是。它是内容证据正例召回率 79/80。因为人工内容标签全部为“是”，没有足够负例，所以不能计算有意义的 Precision 或综合准确率。答辩中必须使用“正例召回”这一准确表述。

### 10. CER 是什么？18.39% 怎么理解？

CER 是字符错误率，把系统转写变成人工参考文本所需的替换、删除和插入次数除以参考字符数。18.39% 说明当前普通话面试场景基本可用，但技术词仍有明显错误，因此页面提供人工修正转写功能。

### 11. 为什么停顿 F1 这么低？

模型时间空隙和人感知的停顿不是完全相同。呼吸、拖音、极短发声、句内/句间差异以及单标注员边界都会造成不一致。当前系统召回了一部分人工停顿，但额外检出较多，所以 Precision 偏低。这个结果说明后续应统一标注定义、加入上下文分类，而不是只改一个阈值。

### 12. 为什么使用字词时间戳，不只使用 VAD？

整句边界容易把一句话内部的卡顿合并掉。字词时间戳能保留词与词之间的空隙，更适合定位面试中的句内停顿；VAD仍用于整体语音检测，两者作用不同。

### 13. 文字转写为什么能离线完成？

因为模型权重已经预先下载到项目的 `models` 目录。运行时 FunASR 和 PyTorch直接读取本地权重并在 CPU 上推理，不需要请求云端接口。

### 14. 为什么第一次分析特别慢？

第一次需要从磁盘加载约 2GB 模型并初始化 PyTorch，之后同一后端进程可以复用模型。可以通过服务启动时预热、保持后端常驻或使用 GPU 推理改善。

### 15. GPU 能解决什么？需要重新训练吗？

GPU 可以加速已有模型推理，不必重新训练。当前代码固定使用 CPU；若使用 GPU，需要安装匹配的 CUDA 版 PyTorch、把设备配置化并重新测速度、显存和 CER。租 GPU 前应先确认是为了演示推理还是研究微调。

### 16. 系统能部署到服务器吗？

能，但当前只配置和测试了单机版。服务端部署还需要修改网络和 CORS、部署模型、增加账号权限、HTTPS、多用户数据库、任务队列和隐私管理。当前 D 盘路径不是硬编码限制。

### 17. 数据如何保护？

录音、人工标注、模型、SQLite 和评测缓存都在 Git 忽略目录，不会进入公开仓库；音频使用 UUID 保存，不使用真实姓名；当前不调用云端分析。答辩展示真人录音前仍需得到明确授权。

### 18. 如何避免数据泄漏和过拟合？

当前 30 条全部明确标为开发集，只用于调整规则。后续的新参与者录音按参与者隔离为测试集，规则锁定后只评估一次。同一个人的录音不能同时出现在开发集和测试集。

### 19. 为什么只有一个岗位？

首版主动收缩范围，先把软件开发岗的题库、规则、语音链路和评测做完整。架构支持增加岗位 JSON 和对应词表，但新岗位必须补充题库审核、领域词汇和真人测试，不能简单复制分数阈值。

### 20. 如果转写错了，后面的评分是不是也会错？

会，ASR 错误会传递到文本分析。因此页面同时保留模型原始转写和用户修正文本。用户修正后重新生成内容报告，但语速和停顿仍保留原始语音计算结果，从而区分“文字识别误差”和“真实表达问题”。

### 21. 关键词出现就一定算回答好吗？

不一定。关键词规则只能提供可解释基线，可能遇到堆砌关键词、否定语境或同义表达漏报。因此我们加入合成负例控制，并在页面展示证据供人检查，而不是把关键词命中当成能力证明。

### 22. 系统出现过什么工程问题？

浏览器录制的 WebM 能播放，但没有容器总时长，旧后端误判文件损坏。我们复现后改为使用音频包时间戳回退计算，并增加自动测试。这体现了从浏览器采集到音频工具链之间的兼容性处理。

### 23. 为什么使用 SQLite？

课程 MVP 是单机演示，SQLite 不需要额外服务器，部署简单并足以保存本机历史。它不适合正式多用户并发；上线时应换成 PostgreSQL 等服务型数据库并增加用户隔离。

### 24. 如果后端在分析中途关闭会怎样？

当前后台任务和 Web 服务同进程，任务可能停在处理中且不会自动恢复。这是已知限制。正式版本应使用独立任务队列、任务重试和状态恢复机制。

### 25. 下一步最重要的工作是什么？

第一，采集 1–2 名未进入开发集的新参与者共 5–10 条录音，形成独立测试集；第二，由 AI 预标注、人工最小复核；第三，锁定规则后只评估一次；第四，补充内容负例和改进 CPU 推理等待。现阶段不优先做大模型微调。

## 十五、答辩时建议主动说明的三句话

1. “我们使用的是预训练语音模型，没有虚构模型训练；创新主要在面试场景的时间戳二次分析、可解释证据和人机修正闭环。”
2. “30 条录音是开发评测集，不是训练集；它帮助我们发现问题、调整规则并量化当前限制。”
3. “系统当前是本地单机训练工具，分数只用于个人复盘，不构成录用判断。”

## 十六、现场演示建议顺序

1. 提前启动后端并完成一次模型预热；
2. 展示软件开发岗题库和题目要点；
3. 使用一条 45–60 秒、已确认可展示的录音；
4. 提交后说明后台正在进行“转码—ASR—时间戳—规则分析”；
5. 展示模型原始转写，现场修正一个技术词并重新评分；
6. 展示题目要点、STAR、证据句和停顿时间轴；
7. 打开历史记录，说明同题趋势只用于个人纵向比较；
8. 主动展示限制和评测指标，避免老师误以为这是录用决策系统；
9. 准备一段离线录屏和一份已完成历史报告，防止现场麦克风或模型冷启动影响演示。
