# 代码现状分析

> 本文件必须在每次 Git 提交前更新。下一次修改前，必须先通读本文件、README.md 和相关代码。

## 当前阶段

阶段 5：真实数据质检与可复现评测，进行中。已使用 30 条匿名开发录音跑通当前 ASR、字词时间戳停顿分析、内容规则与六维人工评分的端到端对照；规则和阈值尚未锁定。

## 当前可运行状态

- Vue 3 前端可在 `http://localhost:5173` 打开；
- FastAPI 后端可在 `http://127.0.0.1:8000` 启动；
- 用户可浏览软件开发岗的 15 道题并按类型筛选；
- 用户可浏览器录音、试听、重录或上传音频；
- 后端支持 WAV、WebM、MP3、M4A、OGG、FLAC，限制为 25MB、5–180 秒；
- 音频会在本机完成 16kHz 单声道标准化、中文转写、VAD、标点恢复和字词时间戳提取；
- 系统使用 `speech-metrics-v2-token-timestamps` 计算有效语速、停顿、最长停顿、停顿占比和语气词等原始指标；
- 离线规则引擎分析题目要点、STAR/逻辑结构、职责/细节、行动、结果和量化事实；
- 报告展示总分、内容/表达分、每项分数依据、证据句、四维雷达图、停顿时间轴和最多三条建议；
- 本机历史页展示已完成报告，支持打开旧报告和查看同题最近 8 次趋势；
- 原始音频、标准化音频、SQLite、模型和依赖均在 Git 忽略目录中。
- `scripts/evaluate-annotations.py` 可读取 UTF-8/GB18030 标注 CSV，批量匹配原始录音、缓存语音分析，并生成不含原始转写的匿名 JSON/Markdown 汇总；
- 室友提供的外部数据集和参考仓库统一保存在本地 `舍友data/`，整个目录已被 Git 忽略，不会进入公开仓库。

## 本次修改

1. 新增阶段 5 标注评测服务和命令行入口，自动定位 7 类 CSV，并兼容 UTF-8、UTF-8 BOM 和 GB18030；
2. 新增任务覆盖、匿名参与者、标注员、评分范围、停顿边界、共识结果、正负例分布和基础隐私格式质检；
3. 新增 ASR 字符错误率 CER、内容三维/总分 MAE、有符号偏差与 Pearson 相关系数；
4. 分别计算“系统原始转写”和“人工修正文本”的内容分，隔离 ASR 错误与内容规则误差；
5. 新增表达三维/总分、综合总分与人工评分对照，并保留每个分项的均值、MAE 和相关系数；
6. 新增人工/系统停顿的一对一时间区间匹配、Precision、Recall、F1，以及 500–1500ms 开发集阈值扫描；
7. 批量音频分析支持按文件大小和修改时间缓存，自动跳过 macOS `._` 资源副本，并兼容重复扩展名；
8. 修复停顿只使用整句边界、无法发现句内卡顿的问题，改用 Paraformer 字词时间戳构造语音段；
9. 新增语音指标版本 `speech-metrics-v2-token-timestamps`，提示旧历史指标不能与新版直接比较；
10. 私人标注包、录音目录和录音压缩包已加入 Git 忽略规则；评测缓存和包含原始转写的中间结果保存在 `runtime/`；
11. 健康检查、前端阶段标识和 API 版本更新到阶段 5 / `0.3.0`；
12. 保留 `rules-v1` 作为不可覆盖基线，新增可合并的 `rules-v2-dev30-syn18`，补充自然口语中的项目背景、职责、行动和结果线索；
13. 新增 18 条明确标记为 AI 合成的内容控制样例，覆盖跑题、关键词堆砌、缺项、集体表述与完整回答，共 136 个布尔断言；
14. 新增 7 项评测、字词时间戳与合成控制测试，自动测试从 12 项增加到 19 项。

## 评分基线

```text
内容分（0–60）=
题目相关性（0–20）+
结构完整性（0–20）+
事实与成果（0–20）

表达分（0–40）=
表达语速（0–10）+
停顿表现（0–15）+
语气词与时长（0–15）

规则基线总分 = 内容分 + 表达分
```

当前报告标明 `rules-v2-dev30-syn18` 和 `development_calibrated_single_annotator`；`rules-v1` 仍作为原始基线保留。当前分数只能用于个人训练和开发集规则实验，不能作为录用判断或通用能力结论。

## 核心 API

| 方法 | 地址 | 作用 |
|---|---|---|
| GET | `/api/health` | 健康检查与阶段状态 |
| GET | `/api/roles` | 岗位列表 |
| GET | `/api/questions` | 按岗位读取题目 |
| GET | `/api/questions/{question_id}` | 题目详情 |
| POST | `/api/sessions` | 创建训练会话 |
| POST | `/api/sessions/{session_id}/answers` | 上传回答并创建分析任务 |
| GET | `/api/answers/{answer_id}` | 查询任务、转写、语音指标和报告 |
| GET | `/api/reports/{answer_id}` | 获取已完成分析报告 |
| GET | `/api/history` | 获取本机历史，可按岗位和题目筛选 |

## 已完成验证

- 后端自动测试：19 项全部通过；
- 覆盖题库、会话、上传、异常格式、语音指标、字词时间戳、标注编码、音频匹配、CER、停顿对齐、离线报告、历史筛选和后台任务持久化；
- Python 依赖检查与已有阶段 3 环境保持可用；
- 前端生产构建：成功，13 个模块完成转换；
- 健康接口返回 `stage: 5` 和 `speech_environment: cpu-ready`；
- 浏览器检查：首页、题库、历史列表、历史报告打开流程均正常；
- 报告浏览器检查：雷达图、六项分数拆解、证据句、STAR 卡片、停顿时间轴、建议和趋势图均正常；
- 浏览器控制台：未发现错误或警告；
- 使用两条明确标记的合成预览记录完成视觉检查，检查后已删除，未保留为真实实验数据；
- Git 差异中的模型、数据库、真实音频、密钥和本地依赖仍处于忽略范围。
- 已使用 Git 忽略规则逐项确认 `舍友data/` 中的 Parquet、CSV、视频和第三方项目文件均不会被跟踪。
- 标注 Excel 共 9 张工作表，已逐张渲染检查；标题、表头、输入区、长文本列和评分标准未发现明显截断。
- 导出后重新读取 Excel，确认 9 张表、360 行停顿预留记录、456 行内容证据记录及总分/持续时间公式均存在；公式错误扫描为 0。
- CSV 任务清单可解析为 36 条记录，覆盖 U001～U006、T01/T02 和 A001～A036，其余 CSV 均为 UTF-8 表头模板。
- 实际返回标注为 GB18030 CSV：30 条内容标注完整，覆盖 5 名匿名参与者；脚本可无乱码读取；
- 30 条原始录音全部在 CPU 上重新完成 ASR 与字词时间戳分析，缓存可重复使用，无音频损坏或模型失败；
- 实测语料级 CER 为 18.39%，样本平均 CER 为 19.67%；
- v1 基线下，人工修正文本内容总分 MAE 为 16.60，端到端原始转写为 16.93；
- `rules-v2-dev30-syn18` 下，上述 MAE 分别降至 9.43 和 9.83，可比内容证据正例召回为 79/80（98.75%）；
- v2 内容总分 Pearson 仍只有 0.0608（人工修正文本）和 0.0704（系统转写），只能说明平均分差缩小，不能证明规则具备排序或泛化能力；
- 18 条 AI 合成控制样例的 136 个布尔断言全部通过；它们只用于规则回归和负例覆盖，不替代真人语音、第二标注员或独立测试集；
- 表达总分 MAE 为 5.33、Pearson 为 0.5491；语气词与时长分相关性相对较好（0.6403），当前语速区间过宽，30 条均得到 10 分；
- 字词时间戳修正后停顿 F1 从 0.1196 提升到 0.1400，但系统仍检出 315 个、人工参考仅 85 个；开发集扫描最佳 600ms 也只有 0.1533，因此暂未锁定新阈值。

测试仍有 2 条来自 FastAPI/Starlette 测试组件的弃用警告，不影响运行和测试通过状态。

## 当前开发环境

阶段 5 本次工作未安装任何新软件、未修改 PATH、未下载新模型、未增加云端配置，也未使用 GPU。

| 项目 | 当前状态 |
|---|---|
| Git | 2.46.1；仓库身份为 dweeqhd / 1351073251@qq.com |
| 原有 Python | `D:\python\python.exe`，3.13.0，未修改 |
| 项目 Python | `.tools\python312`，3.12.10 |
| Python 虚拟环境 | `.venv` |
| Node.js / npm | Node 24.21.0 / npm 11.19.0，位于 `.tools` |
| FFmpeg | 9.0.1，位于 `.tools` |
| PyTorch / torchaudio | 2.11.0+cpu |
| FunASR / ModelScope | 1.4.15 / 1.40.0 |
| 语音模型 | Paraformer 约 0.93GB、FSMN-VAD 小于 0.01GB、CT-Punc 约 1.11GB |
| 内容分析 | 本地 `rules-v2-dev30-syn18`（继承 v1），无云端调用 |
| 硬件 | i7-13620H、约 16GB 内存、无 NVIDIA CUDA 显卡 |

## 当前限制与风险

1. 题库和规则词表仍为 AI 草案，必须人工审核；
2. 关键词命中不等于真正掌握技能；实测内容总分相关性较低，结构和事实规则会漏报省略主语、非固定连接词等自然口语；
3. ASR 错字会传递到内容分析，当前还不能在页面修正转写；
4. 数字出现不一定是项目成果，系统目前只能保留证据供人复核；
5. 当前 120–300 表达单位/分钟的语速区间过宽，本批 30 条全部得到 10 分，无法区分人工评分；
6. 停顿时间戳已改为字词级，但人工/系统停顿 F1 仍只有 0.1400，阈值扫描不足以解决边界和标注定义差异；
7. 当前规则基线可能给结构化合成文本较高分，不能展示为真实性或能力证明；
8. 返回数据只有 5 人/30 条，少于计划 6 人/36 条；A028～A030 缺少停顿表行；
9. 当前只能提供 L01 一名标注员且共识表为空，无法计算标注者间一致性；本项目接受这一现实限制并在报告中显式披露；
10. 90 条内容证据标签全部为“是”，无法估计内容规则误报率；
11. 历史记录没有账号隔离，只适合单机课程演示；
12. 后台任务与 Web 服务同进程，服务中断后排队任务不会自动恢复；
13. CPU 冷启动模型约需 80 秒，页面需要等待；
14. 还没有转写修正、训练记录删除入口、访问控制和正式数据保留周期；
15. `speech-metrics-v2-token-timestamps` 与旧语音指标不可直接比较，后续需要报告版本迁移策略；
16. 语音模型当前使用下载时的 `master` 修订，最终归档前应固定不可变修订并复核许可证；
17. 私人标注原文和录音不得提交或上传公开链接；脚本只把匿名汇总写入报告。

## 版本管理状态

- 阶段 1 提交：`7841de5`；
- 阶段 2 提交：`d77166b`；
- 阶段 3 提交：`50ba4af`；
- 本文件所在提交为阶段 5 首轮真实数据评测实现；
- GitHub 公开仓库已创建：`https://github.com/dweeqhd/ai-interview-training-system`；
- 阶段 1–4 已推送到 `origin/main`，阶段 4 功能提交为 `ddadd89`。

## 需要人工完成

1. 补齐 U006 对应的 A031～A036 六条录音和完整标注，或明确把任务规模正式调整为 5 人/30 条；
2. 补齐 A028～A030 的停顿标注表行；
3. 在内容证据表中保留真实的“否”样本，不要因为题目希望出现某要点就全部填写“是”；
4. 复核人工停顿定义和时间边界，重点复听系统检出但人工未标的样本，以及人工长停顿内仍存在短发声的样本；
5. 另采集按参与者隔离的独立测试集；锁定规则后，测试集只评估一次；
6. 继续审核题库和规则同义线索，但不要直接用当前单标注、全正例数据宣称规则准确率；
7. 真实音频和私人标注只放在 Git 忽略目录，不得推送到公开 GitHub 仓库。

## 下一阶段

阶段 5：真实数据评测、规则校准、隐私完善与答辩稳定性。

建议顺序：

1. 把现有 30 条数据正式定义为单标注员开发集，补齐缺失停顿行和可确认的内容负例后重新评测；不虚构第二标注员或共识结果；
2. 基于分歧复核结果统一“人工感知停顿”定义，再决定 VAD/字词时间戳匹配策略和阈值；
3. 保留已生成的 v2 开发版本和 AI 合成控制集，继续审核自然口语同义线索，不覆盖 `rules-v1`；
4. 增加网页转写修正与重新分析，降低 18.39% CER 对内容反馈的传递影响；
5. 增加删除训练记录与音频的可控入口；
6. 锁定规则和指标版本后，在独立测试集生成最终实验表；
7. 修复高频误报，准备弱回答/改进回答演示案例；
8. 如后续确需语音微调或噪声鲁棒性实验，把新增公开语音控制在约 30–60 分钟、几百 MB 以内，并选择能在 10 小时内完成的轻量方案；在明确任务和基线前不租 GPU。

## 下一阶段配置申请

阶段 5 的数据导入、离线评测、规则校准、删除入口和答辩稳定性工作使用现有环境即可，不需要安装新软件。

以下两项仍未获批准，默认不执行：

- 云端大语言模型：需要单独批准具体服务、API 密钥、费用和允许发送的数据；
- GPU 训练：当前 30 条单标注开发数据不适合训练，暂不需要租卡；若后续实验证明确实需要，只采用 10 小时内可完成的小样本轻量方案，并在租卡前单独确认训练目标、显存、平台、数据传输范围和预算。
