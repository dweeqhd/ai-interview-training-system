# 代码现状分析

> 本文件必须在每次 Git 提交前更新。下一次修改前，必须先通读本文件、README.md 和相关代码。

## 当前阶段

阶段 2：前后端最小系统骨架，已完成。

## 当前可运行状态

- Vue 3 前端可在 `http://localhost:5173` 打开；
- FastAPI 后端可在 `http://127.0.0.1:8000` 启动；
- 已实现健康检查、岗位列表、题目列表和题目详情接口；
- 前端可加载软件开发岗的 15 道题并按题目类型筛选；
- 录音按钮目前有意禁用，阶段 3 才接入录音、上传和分析任务；
- 尚未安装 FFmpeg、FunASR、PyTorch，尚未下载语音模型；
- 题库仍为 AI 初稿，尚未经过人工审核；
- 数据目录中只有空白模板，没有真实录音和人工标注。

## 本次修改

1. 创建 Vue 3 + Vite 前端骨架和“职镜”题库浏览首页；
2. 创建 FastAPI 后端，读取本地题库并提供 4 个 API；
3. 增加岗位选择、题目分类筛选、加载态和错误态；
4. 增加 6 个后端 API 自动测试；
5. 增加前后端启动脚本与一键测试脚本；
6. 安装项目专用 Python、Node.js 和阶段 2 依赖，全部放在 D 盘项目目录；
7. 更新 README，补充运行、测试和环境说明；
8. 将 `.tools/` 加入 Git 忽略列表，避免提交本地运行时。

## 已完成验证

- 后端自动测试：6 项全部通过；
- 前端生产构建：成功；
- 联调健康检查：返回 `status=ok`、`stage=2`；
- 联调岗位接口：返回 1 个软件开发岗；
- 联调题目接口：返回 15 道题；
- 浏览器人工检查：首页布局、中文内容、岗位与题库区域显示正常；
- 真实录音、私有标注、模型和本地依赖仍被 Git 忽略。

测试有 2 条来自第三方测试组件的弃用警告，目前不影响运行和测试结果，后续依赖升级时处理。

## 当前开发环境检查结果

| 项目 | 当前状态 | 是否现在处理 |
|---|---|---|
| Git | 2.46.1；仓库身份为 dweeqhd / 1351073251@qq.com | 已完成 |
| 原有 Python | `D:\python\python.exe`，3.13.0 | 保持不变 |
| 项目 Python | `.tools\python312`，3.12.10 | 已按用户授权安装 |
| Python 虚拟环境 | `.venv` | 已按用户授权创建 |
| Node.js / npm | `.tools\node-v24.21.0-win-x64`，Node 24.21.0 / npm 11.19.0 | 已按用户授权安装 |
| 后端依赖 | FastAPI、Uvicorn、SQLAlchemy、pytest、httpx、python-multipart | 已按用户授权安装 |
| 前端依赖 | Vue、Vite、ECharts | 已安装在项目目录 |
| FFmpeg / FunASR / PyTorch | 未安装 | 阶段 3 需再次征得用户同意 |
| 语音模型 | 未下载 | 阶段 3 需再次征得用户同意 |
| 硬件 | i7-13620H、约 16GB 内存、无 NVIDIA CUDA 显卡 | 阶段 3 建议 CPU 方案 |

## 版本管理状态

- 已按用户授权配置当前仓库专用的 Git 提交身份；
- GitHub 当前停留在用户二步验证页面，远程仓库尚未创建；
- 完成阶段 2 本地提交后，待 GitHub 登录验证完成再创建并推送；
- 计划创建默认私有仓库 ai-interview-training-system。

## 需要人工完成

1. 审核 data/question_bank/software_developer.draft.json 中的 15 道题；
2. 确认保留、修改或删除哪些问题；
3. 按 docs/02-人工数据准备指南.md 先录制 5 人 × 3 题的开发样本；
4. 使用模板记录知情同意；
5. 在开始真实数据采集前，确定数据保留周期。

## 下一阶段

阶段 3：接入回答录音、上传、会话存储和异步分析任务骨架。

计划内容：

1. 浏览器录音、计时、试听、重录与上传；
2. SQLite 保存训练会话、回答和分析任务状态；
3. 后端校验音频格式、时长和大小；
4. 建立“待处理—处理中—成功/失败”的任务流程；
5. 接入 FFmpeg 做音频标准化；
6. 接入 FunASR 的语音识别、VAD 停顿切分和标点恢复；
7. 先输出转写文本、总时长、语速和停顿等基础结果。

## 下一阶段开始前需要用户确认的环境配置

阶段 3 配置申请（尚未执行，等待用户明确同意）：

1. 在项目 `.tools` 中放置 FFmpeg，不写入系统 PATH；
2. 在现有 `.venv` 中安装 CPU 版 PyTorch 与 torchaudio；
3. 在现有 `.venv` 中安装 FunASR、ModelScope 及其必要依赖；
4. 将中文 Paraformer ASR、FSMN-VAD 和 CT-Transformer 标点模型下载到项目 `models` 或 D 盘专用缓存；
5. 模型只在本机使用，不提交 Git；
6. 暂不安装 CUDA，不训练大型模型。

以上配置的具体版本、下载量和磁盘占用会在执行前再次核对。未经用户同意不得开始。

## 后续可选训练路线

用户可以租用云端 GPU，或使用舍友带独立显卡的电脑训练。该资源暂不作为阶段 3 的必需条件：先用本机 CPU 和预训练模型做出可运行基线；积累并人工标注真实面试数据后，在阶段 5 评估微调轻量文本评分模型、停顿分类模型或参数高效微调方案。训练产物需保留数据授权、实验参数、基线对照和评测结果，才能作为答辩中的“算法改进”证据。
