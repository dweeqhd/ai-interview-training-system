# 代码现状分析

> 本文件必须在每次 Git 提交前更新。下一次修改前，必须先通读本文件、README.md 和相关代码。

## 当前阶段

阶段 4：离线内容分析、可解释报告与训练历史，已完成。

## 当前可运行状态

- Vue 3 前端可在 `http://localhost:5173` 打开；
- FastAPI 后端可在 `http://127.0.0.1:8000` 启动；
- 用户可浏览软件开发岗的 15 道题并按类型筛选；
- 用户可浏览器录音、试听、重录或上传音频；
- 后端支持 WAV、WebM、MP3、M4A、OGG、FLAC，限制为 25MB、5–180 秒；
- 音频会在本机完成 16kHz 单声道标准化、中文转写、VAD 和标点恢复；
- 系统计算有效语速、停顿、最长停顿、停顿占比和语气词等原始指标；
- 离线规则引擎分析题目要点、STAR/逻辑结构、职责/细节、行动、结果和量化事实；
- 报告展示总分、内容/表达分、每项分数依据、证据句、四维雷达图、停顿时间轴和最多三条建议；
- 本机历史页展示已完成报告，支持打开旧报告和查看同题最近 8 次趋势；
- 原始音频、标准化音频、SQLite、模型和依赖均在 Git 忽略目录中。

## 本次修改

1. 新增 `data/analysis_rules/rules.v1.json`，保存要点同义线索、STAR/逻辑结构线索和暂定表达阈值；
2. 新增离线内容分析引擎，输出规则版本、匹配词、证据句、原始指标、分项分数和免责声明；
3. 项目/行为题使用 STAR 四项规则，技术题使用“观点—方法—权衡—验证”四项逻辑规则；
4. 新增职责/具体细节、具体行动、结果/验证和量化事实检测；
5. 新增 60 分内容基线与 40 分表达基线，保留六个二级分及每项计算依据；
6. 新增 `analysis_reports` 数据表，报告 JSON 与规则版本和回答一一关联；
7. 后台语音任务完成后自动生成并持久化内容报告；
8. 新增报告详情和本机历史接口，历史支持岗位、题目和数量筛选；
9. 前端新增完整报告组件：雷达图、分数拆解、证据列表、结构卡片、表达指标、停顿时间轴和建议；
10. 前端新增历史组件和同题趋势图，不引入新图表依赖；
11. 健康检查阶段号由 3 更新为 4；
12. 新增阶段 4 算法、评分、局限性和人工校准说明；
13. 自动测试由 9 项增加到 12 项。

## 评分基线

```text
内容分（0–60）=
题目相关性（0–20）+
结构完整性（0–20）+
事实与成果（0–20）

表达分（0–40）=
表达语速（0–10）+
停顿表现（0–15）+
语气词与时长（0–15）

规则基线总分 = 内容分 + 表达分
```

所有报告均标明 `rules-v1` 和 `ai_draft_pending_human_calibration`。当前分数只能用于个人训练和规则基线实验，不能作为录用判断或通用能力结论。

## 核心 API

| 方法 | 地址 | 作用 |
|---|---|---|
| GET | `/api/health` | 健康检查与阶段状态 |
| GET | `/api/roles` | 岗位列表 |
| GET | `/api/questions` | 按岗位读取题目 |
| GET | `/api/questions/{question_id}` | 题目详情 |
| POST | `/api/sessions` | 创建训练会话 |
| POST | `/api/sessions/{session_id}/answers` | 上传回答并创建分析任务 |
| GET | `/api/answers/{answer_id}` | 查询任务、转写、语音指标和报告 |
| GET | `/api/reports/{answer_id}` | 获取已完成分析报告 |
| GET | `/api/history` | 获取本机历史，可按岗位和题目筛选 |

## 已完成验证

- 后端自动测试：12 项全部通过；
- 覆盖题库、会话、上传、异常格式、语音指标、离线报告、报告接口、历史筛选和后台任务持久化；
- Python 依赖检查与已有阶段 3 环境保持可用；
- 前端生产构建：成功，13 个模块完成转换；
- 健康接口返回 `stage: 4` 和 `speech_environment: cpu-ready`；
- 浏览器检查：首页、题库、历史列表、历史报告打开流程均正常；
- 报告浏览器检查：雷达图、六项分数拆解、证据句、STAR 卡片、停顿时间轴、建议和趋势图均正常；
- 浏览器控制台：未发现错误或警告；
- 使用两条明确标记的合成预览记录完成视觉检查，检查后已删除，未保留为真实实验数据；
- Git 差异中的模型、数据库、真实音频、密钥和本地依赖仍处于忽略范围。

测试仍有 2 条来自 FastAPI/Starlette 测试组件的弃用警告，不影响运行和测试通过状态。

## 当前开发环境

阶段 4 未安装任何新软件、未修改 PATH、未下载新模型、未增加云端配置。

| 项目 | 当前状态 |
|---|---|
| Git | 2.46.1；仓库身份为 dweeqhd / 1351073251@qq.com |
| 原有 Python | `D:\python\python.exe`，3.13.0，未修改 |
| 项目 Python | `.tools\python312`，3.12.10 |
| Python 虚拟环境 | `.venv` |
| Node.js / npm | Node 24.21.0 / npm 11.19.0，位于 `.tools` |
| FFmpeg | 9.0.1，位于 `.tools` |
| PyTorch / torchaudio | 2.11.0+cpu |
| FunASR / ModelScope | 1.4.15 / 1.40.0 |
| 语音模型 | Paraformer 约 0.93GB、FSMN-VAD 小于 0.01GB、CT-Punc 约 1.11GB |
| 内容分析 | 本地 `rules-v1`，无云端调用 |
| 硬件 | i7-13620H、约 16GB 内存、无 NVIDIA CUDA 显卡 |

## 当前限制与风险

1. 题库和规则词表仍为 AI 草案，必须人工审核；
2. 关键词命中不等于真正掌握技能，规则可能误报或漏报同义表达；
3. ASR 错字会传递到内容分析，当前还不能在页面修正转写；
4. 数字出现不一定是项目成果，系统目前只能保留证据供人复核；
5. 语速、停顿、语气词和时长阈值尚未用人工数据校准；
6. 当前规则基线可能给结构化合成文本较高分，不能展示为真实性或能力证明；
7. 历史记录没有账号隔离，只适合单机课程演示；
8. 后台任务与 Web 服务同进程，服务中断后排队任务不会自动恢复；
9. CPU 冷启动模型约需 80 秒，页面需要等待；
10. 还没有训练记录删除入口、访问控制和正式数据保留周期；
11. 不同规则版本的历史分数不能直接比较，后续需要报告版本迁移策略；
12. 语音模型当前使用下载时的 `master` 修订，最终归档前应固定不可变修订并复核许可证。

## 版本管理状态

- 阶段 1 提交：`7841de5`；
- 阶段 2 提交：`d77166b`；
- 阶段 3 提交：`50ba4af`；
- 本文件所在提交为阶段 4 实现提交；
- GitHub 公开仓库已创建：`https://github.com/dweeqhd/ai-interview-training-system`；
- 阶段 1–3 已推送，阶段 4 提交完成后推送到 `origin/main`。

## 需要人工完成

1. 审核 `data/question_bank/software_developer.draft.json` 的 15 道题；
2. 审核 `data/analysis_rules/rules.v1.json` 中每个题目要点的同义线索；
3. 使用一段本人非敏感录音验证麦克风、转写、等待体验和报告结论；
4. 按 `docs/02-人工数据准备指南.md` 采集 5 人 × 3 题的第一批开发样本；
5. 每段建议 30–120 秒，文件不得包含真实姓名，必须记录知情同意；
6. 至少选择 10 条音频，由两人独立标注明显停顿、题目要点、结构与流畅度；
7. 逐条对照报告的 `evidence`，记录要点、STAR、职责、行动、结果和量化事实的误报/漏报；
8. 真实音频和私人标注只放在 Git 忽略目录，不得推送到公开 GitHub 仓库。

## 下一阶段

阶段 5：真实数据评测、规则校准、隐私完善与答辩稳定性。

建议顺序：

1. 等团队提供经过同意和匿名处理的开发集；
2. 增加转写修正与重新分析；
3. 增加删除训练记录与音频的可控入口；
4. 编写停顿检测、内容规则与人工评分对照脚本；
5. 只使用开发集调整词表和阈值；
6. 锁定规则版本后在测试集上生成真实实验表；
7. 修复高频误报，准备弱回答/改进回答演示案例；
8. 再决定是否进行轻量模型训练或接入云端大模型。

## 下一阶段配置申请

阶段 5 的数据导入、离线评测、规则校准、删除入口和答辩稳定性工作使用现有环境即可，不需要安装新软件。

以下两项仍未获批准，默认不执行：

- 云端大语言模型：需要单独批准具体服务、API 密钥、费用和允许发送的数据；
- GPU 训练：需要在人工数据到位后单独确认训练目标、租卡平台或舍友电脑环境、数据传输范围和预算。
