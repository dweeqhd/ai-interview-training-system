# 代码现状分析

> 本文件必须在每次 Git 提交前更新。下一次修改前，必须先通读本文件、README.md 和相关代码。

## 当前阶段

阶段 3：录音、上传、会话存储与基础语音分析，已完成。

## 当前可运行状态

- Vue 3 前端可在 `http://localhost:5173` 打开；
- FastAPI 后端可在 `http://127.0.0.1:8000` 启动；
- 用户可浏览软件开发岗的 15 道题并按类型筛选；
- 点击“开始练习”可打开录音区，支持浏览器录音、试听、重录或上传音频；
- 后端支持 WAV、WebM、MP3、M4A、OGG、FLAC，限制为 25MB、5–180 秒；
- SQLite 保存训练会话、回答、分析任务状态、转写文本和原始指标；
- 后台任务会把音频转为 16kHz 单声道 WAV，再运行中文 ASR、VAD 和标点恢复；
- 页面可展示转写文本、有效语速、明显停顿、最长停顿和语气词计数；
- 原始音频、标准化音频、SQLite、模型和依赖均在 Git 忽略目录中。

## 本次修改

1. 安装项目本地 FFmpeg 9.0.1，不修改系统 PATH；
2. 在项目 `.venv` 安装 PyTorch/torchaudio 2.11.0 CPU、FunASR 1.4.15、ModelScope 1.40.0；
3. 下载中文 Paraformer、FSMN-VAD、CT-Punc 到 D 盘项目模型缓存；
4. 新增 SQLAlchemy 数据库、训练会话、回答和分析任务模型；
5. 新增创建会话、上传回答、查询回答与任务状态 API；
6. 新增音频大小、格式、时长检查和 FFmpeg 标准化；
7. 新增 FunASR 懒加载、本地模型路径优先和 CPU 推理；
8. 新增语速、停顿、语气词等可解释指标计算；
9. 前端增加录音、文件上传、试听、任务轮询和基础结果展示；
10. 增加阶段 3 环境检查、模型下载脚本和技术说明文档；
11. 自动测试由 6 项增加到 9 项。

## 已完成验证

- `pip check`：未发现依赖冲突；
- 环境导入：torch、torchaudio、FunASR、ModelScope 均成功；
- CUDA 检查：`False`，符合本机 CPU 方案；
- FFmpeg：9.0.1 可执行；
- 后端自动测试：9 项全部通过；
- Python 编译检查：通过；
- 前端生产构建：成功；
- 浏览器检查：首页、15 道题、练习区、录音与上传入口显示正常；
- 浏览器控制台：未发现错误或警告；
- 模型自带公开中文样例直接推理：成功；
- 完整 API 联调：创建会话、上传、排队、处理、完成和结果查询全部成功；
- 联调结果：6.52 秒样例得到中文转写、246.0 表达单位/分钟、停顿与语气词明细；
- 冷启动端到端约 83 秒，模型加载后的单条 6.52 秒音频推理约 1 秒；
- 真实音频、模型、数据库、密钥和本地依赖均未进入 Git 跟踪范围。

测试仍有 2 条来自 FastAPI/Starlette 测试组件的弃用警告，不影响运行和测试通过状态。

## 当前开发环境

| 项目 | 当前状态 |
|---|---|
| Git | 2.46.1；仓库身份为 dweeqhd / 1351073251@qq.com |
| 原有 Python | `D:\python\python.exe`，3.13.0，未修改 |
| 项目 Python | `.tools\python312`，3.12.10 |
| Python 虚拟环境 | `.venv` |
| Node.js / npm | Node 24.21.0 / npm 11.19.0，位于 `.tools` |
| FFmpeg | 9.0.1，位于 `.tools` |
| PyTorch / torchaudio | 2.11.0+cpu |
| FunASR / ModelScope | 1.4.15 / 1.40.0 |
| 语音模型 | Paraformer 约 0.93GB、FSMN-VAD 小于 0.01GB、CT-Punc 约 1.11GB |
| 硬件 | i7-13620H、约 16GB 内存、无 NVIDIA CUDA 显卡 |

## 核心 API

| 方法 | 地址 | 作用 |
|---|---|---|
| GET | `/api/health` | 健康检查与阶段状态 |
| GET | `/api/roles` | 岗位列表 |
| GET | `/api/questions` | 按岗位读取题目 |
| GET | `/api/questions/{question_id}` | 题目详情 |
| POST | `/api/sessions` | 创建训练会话 |
| POST | `/api/sessions/{session_id}/answers` | 上传回答并创建分析任务 |
| GET | `/api/answers/{answer_id}` | 查询任务、转写和指标 |

## 当前限制与风险

1. 题库仍为 AI 草案，正式测试前必须人工审核；
2. 停顿阈值 500ms、长停顿阈值 1500ms 尚未用人工数据校准；
3. “然后、就是”等词可能是正常连接词，目前只计数、不扣分；
4. ASR 错字会影响表达单位和语气词统计；
5. CPU 冷启动模型约需 80 秒，页面需要等待；
6. 后台任务与 Web 服务同进程，服务中断后排队任务不会自动恢复；
7. 当前无账号和访问控制，只适合本地课程演示；
8. 还没有 STAR、题目相关性、成果表达等内容分析；
9. 还没有历史记录页、趋势图和正式报告页；
10. 模型当前使用下载时的 `master` 修订，最终归档前应记录不可变修订并复核模型许可证。

## 版本管理状态

- 阶段 1 本地提交：`7841de5`；
- 阶段 2 本地提交：`d77166b`；
- 本文件所在提交为阶段 3 实现提交；
- GitHub 远程仓库仍未创建；此前登录停在用户二步验证环节；
- 计划创建默认私有仓库 `ai-interview-training-system`，完成登录后推送全部提交。

## 需要人工完成

1. 审核 `data/question_bank/software_developer.draft.json` 的 15 道题；
2. 先使用一段本人非敏感录音验证麦克风、转写和等待体验；
3. 按 `docs/02-人工数据准备指南.md` 采集 5 人 × 3 题的小规模开发样本；
4. 每段建议 30–120 秒，文件不得包含真实姓名，必须记录知情同意；
5. 至少选择 10 条音频，由两人独立标注明显停顿和流畅度；
6. 将人工认为的停顿与系统 `metrics.pauses` 对比，不要直接接受当前阈值；
7. 真实音频只放在被 Git 忽略的私有数据目录，不得提交 GitHub。

## 下一阶段

阶段 4：内容分析、报告页与训练历史。

计划内容：

1. 基于题目 expected_points 做关键词/要点覆盖；
2. 建立可解释的 STAR 结构规则基线；
3. 增加个人职责、具体行动和量化结果检测；
4. 组合表达指标与内容指标，但保留每个原始证据；
5. 制作报告页、雷达图、停顿时间轴和优先改进建议；
6. 增加会话历史与同题多次训练趋势；
7. 使用人工样本校准阈值并输出对照实验表。

## 下一阶段配置申请

阶段 4 的规则基线与报告页不需要安装新软件，现有环境即可完成。

可选项是接入大语言模型生成更自然的内容建议，这会涉及 OpenAI API 密钥、费用和将转写文本发送给云端。未得到用户对“具体数据、发送目的地和费用”的单独明确同意前，不创建密钥、不写入 `.env`、不调用外部模型。建议阶段 4 先完成完全离线、可解释的规则基线，再单独决定是否增加云端 AI 建议。

## 后续可选训练路线

用户可以租用云端 GPU，或使用舍友带独立显卡的电脑训练。建议在阶段 5、积累并人工标注数据之后，再评估轻量文本评分模型、停顿分类模型或参数高效微调。必须保留数据授权、数据划分、训练参数、基线对照和测试集结果，才能作为答辩中的算法改进证据。
