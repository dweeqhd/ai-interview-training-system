# 代码现状分析

> 本文件必须在每次 Git 提交前更新。下一次修改前，必须先通读本文件、README.md 和相关代码。

## 当前阶段

阶段 1：需求、范围与数据规范。

## 当前可运行状态

- 当前尚无前端或后端代码；
- 当前不需要启动服务；
- 未安装任何项目依赖；
- 尚未下载任何语音模型；
- 题库为 AI 初稿，尚未经过人工审核；
- 数据目录中只有空白模板，没有真实录音和人工标注。

## 本次修改

1. 创建项目 README 和强制协作规则 AGENTS.md；
2. 固化 MVP 功能边界、评分维度和验收标准；
3. 创建 15 道软件开发岗面试题草案；
4. 创建录音元数据、人工评分、停顿标注 CSV 模板；
5. 创建录音使用同意书模板；
6. 创建 gitignore，排除音频、密钥、模型、数据库和依赖目录；
7. 建立“修改前通读、提交前更新本文件”的流程。

## 已完成验证

- 已确认计划文件存在并已阅读；
- 已验证题库 JSON 格式有效，共 15 道题；
- 已确认 data/templates 下有 4 个数据与同意书模板；
- 当前项目文件清单共 12 个文件；
- 已初始化 main 分支的本地 Git 仓库；
- 已验证 Git 会忽略真实音频、私有标注和 .env；
- 未进行任何环境安装或系统配置。

## 当前开发环境检查结果

| 项目 | 当前状态 | 是否现在处理 |
|---|---|---|
| Git | 已安装，版本 2.46.1 | 可直接使用 |
| Node.js | Codex 内置运行时可见，版本 24.19.0 | 不作为项目环境使用 |
| npm | 当前命令不可用 | 阶段 2 建议安装 Node.js LTS（自带 npm），需用户同意 |
| Python | 仅检测到 3.13.0 | 阶段 2 建议另装 Python 3.12 并创建项目虚拟环境，需用户同意 |
| FFmpeg | 当前命令不可用 | 阶段 2 前需用户同意后安装或配置 |
| GitHub CLI | 未安装 | 本次使用浏览器创建仓库，不安装 CLI |

## 版本管理状态

- 已按用户授权配置当前仓库专用的 Git 提交身份；
- GitHub 浏览器自动化当前无法稳定打开创建页面，远程仓库尚未创建；
- 本地提交将先正常保留，待 GitHub 创建成功后统一推送；
- 计划创建默认私有仓库 ai-interview-training-system。

## 需要人工完成

1. 审核 data/question_bank/software_developer.draft.json 中的 15 道题；
2. 确认保留、修改或删除哪些问题；
3. 按 docs/02-人工数据准备指南.md 先录制 5 人 × 3 题的开发样本；
4. 使用模板记录知情同意；
5. 在开始真实数据采集前，确定数据保留周期。

## 下一阶段

阶段 2：建立前后端最小系统骨架。

计划内容：

1. 创建 Vue 3 前端；
2. 创建 FastAPI 后端；
3. 提供健康检查、岗位列表和题目列表接口；
4. 完成岗位选择和题目展示页面；
5. 建立自动测试和一键启动说明。

## 下一阶段开始前需要用户确认的环境配置

阶段 2 的最小配置申请：

1. 安装 Node.js LTS，以获得稳定的 Node.js 和 npm；
2. 安装 Python 3.12，与现有 Python 3.13 并存；
3. 在项目内创建 .venv，不修改现有 Python 包；
4. 在 .venv 中安装 FastAPI、Uvicorn、SQLAlchemy、pytest、httpx、python-multipart；
5. 暂不安装 FFmpeg、FunASR、PyTorch，暂不下载模型，留到阶段 3 单独申请。

仓库级 Git user.name 和 user.email 已按用户提供的信息配置完成。
